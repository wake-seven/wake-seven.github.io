# 開発の引き継ぎ

## 判断基準

- 公開中かつ開発の正本は、ルートの `index.html` 1枚。CSS・DOM・JavaScriptを外部ソースへ分割せず、ビルド工程も追加しない。`all-patterns.html` と `index_3D.html` は報酬用の付属ページ。
- 最優先の目的は、人とAIが機能追加・故障修正を速く正確にできること。見た目だけの整理、抽象化、疎結合化を目的に改修箇所を散らさない。共通処理が必要なら、その機能から自然に追える接続点を残す。
- 新しい依頼では、依頼文から自分で日本語・英語の自然な語を選んで `rg` する。専用の検索キーや仕様IDを知っている前提にしない。見つけた周辺から、問題データ、状態、判定、表示、操作、演出、保存への影響を追い、必要な場所を局所的に直す。検索用だけのコメントや別名を増やさない。
- 現在の挙動とユーザーが明示した変更を基準にする。旧版の計画・検証記録は現行仕様の正本ではない。判断できなければ、実画面と該当コードを確認する。
- 既存の変更は他の作業の可能性がある。着手時に `git status` を見て、無関係な差分を消さない。

## 作業時の確認

- 改修前後に、依頼に関係する処理を自然な語で探せるか、検索地点の周辺から変更の目的と影響が読めるかを確認する。単に短いコードや整った分割になったことを成功条件にしない。
- 画面文言を変える場合は同じ機能の日本語・英語・中国語・韓国語を確認する。翻訳は `registerLocalizedTextGroup` など機能ごとの定義を使い、表示箇所に一言語だけを直書きしない。
- 保存キーは `wake7-rebuild-v2`。保存形式や解放条件を変える場合は、復元・通常進行・二周目・速解きへの影響を確認する。利用者の保存データを無断で消さない。
- 表示はローカルHTTPで `index.html` を開いて確認する。`?debug=1` には状態移動と起動時自己検査があり、通常表示では自己検査を実行しない。自己検査の成功だけで見た目や操作を確認済みにせず、関係する画面・操作・リロードを確認する。

直近の未修正点: 英語の発展クラスのクリア画面で、右上の `Advanced Class 1 / 8` の末尾が見切れる（2026-09-26の実画面確認）。
