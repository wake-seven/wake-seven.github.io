# Wake Seven 再構築用・観測仕様

> 用途: 現行 `index.html` を正本として、手編集する単一HTML版を再構築するための使い捨て台帳。恒久ドキュメントではない。  
> 方針: 実装の構造ではなく、画面で観測できる挙動・状態遷移・保存契約を記す。完了後に削除可。  
> 調査対象: `index.html`（2026-09-06 時点）。README は対象外。

## 読み方

- ID は実装コメント・検証チェックで検索できる識別子。例: `APP-INTRO-001`。
- **根拠**は、現行HTMLで再調査する入口（関数名・検索語・行番号）である。行番号は現行版の目安で、改修によりずれる。
- 「要確認」は、コードから契約を一意に断定できない、または実機で見た目を確認すべき事項である。
- 「現行の不足」と明記した箇所は、現行コードに競合余地があるため、再構築ではそのまま再現せず合否条件として扱う。
- 初期の新実装では互換性を持たせない。既存の `localStorage`、途中セッション、解放状態を移行しない前提でよい。ただし以下の**新実装内での再読み込み復帰**は必要。

## 0. 共通のゲーム定義

### BOARD-001 七枚盤面と状態

- 盤面は七枚の六角形パネルからなる。各パネルのだるまは起きた状態を含む三段階の向きを持つ。
- 一回の操作は、隣接する三枚を選び、軸を中心に 120 度の一方向回転を確定すること。
- 全員が起きた状態をクリアとする。最短手数は全状態探索の距離で決まり、個々のステージは初期状態と最短手数 (`par`) を持つ。
- 盤面の見た目の回転（画面の向き変更）と、ゲーム上の一手は区別する。

**根拠:** `WakeSevenBoardDomain`、`SOLVER=buildSolver('roll')`、`isSolved()`（2048-2057, 5157）、`rollOnce`、`enc`、`dec`。
**要確認:** 3値のうちどの寝方が画面上でどの向きに対応するかは、再構築時に現行盤面とスクリーンショット比較で固定する。

### BOARD-002 基本操作と入力制約

- 水色の棒（つかみどころ）をポインタ／タッチでドラッグし、しきい値を超えた向きで 120 度単位の回転を確定する。
- ドラッグ中は選択した三枚だけが連続回転して見え、手数・残り手数も仮表示へ更新される。
- 盤面が回転中、到着演出中、導入見本中、クリア演出中、またはモーダル表示中には、新しい盤面操作を受け付けない。
- `pointercancel`、盤面外へ出る、短いフリックなどでも、状態を壊さず元へ戻すか一手として確定する。
- タッチ操作中はページスクロールを抑止する。

**根拠:** `handleBoardPointerDown/Move/Up/Cancel`、`finishDrag`（7803-8223, 7969-8109）、`boardTouchActive`、`handleBoardTouchMove`（8222）。  
**現行の不足:** `handleBoardPointerDown` の明示ロックは `busy` と `.arriving` が中心で、通常クリア中の `clearShown` や `applicationTargetRevealPending` は拒否条件に含まれない（7803-7829）。棒を透明化しても `gripAt()` は座標で判定するため、再構築では表示の透明化を入力ロックの代用にしない。

### BOARD-003 一手戻す・やり直す

- 通常の許可モードでは、確定した手は履歴から一手戻せる。戻すと盤面、手数、並び順、必要に応じて残り手数表示も戻る。
- 「やり直す」はその問題の初期盤面へ戻す。アニメーション中は受け付けない。
- 悟り最終問題は一手戻し不可。速解きは派生モード定義により許可／不許可を決める。
- 学習補助の不正操作を戻す自動巻き戻しは、ユーザーの履歴操作とは別で、誤手を履歴へ残さない。

**根拠:** `undoLastMove`（8225）、`restartWithAnimation`（7739）、`speedAllowsUndo`（5294）、`isFinalMasterPuzzle`（5160）、`animateGuidedBasicRewind`（8143）。

### BOARD-004 共通の到着・クリア演出

- 問題を切り替えたときの実盤面は、中央から各パネルが広がって現れる到着演出を使う。ただし応用クラス開始時の「完成形見本」が先にある場合は、その見本にこの演出を重ねない。
- クリア時は全員を笑顔にし、盤面の揺れと中央からのリング／バーストを再生する。これはクリア時だけであり、通常の「寝→起」判定変化で起きただるまを個別にフラッシュさせない。
- `prefers-reduced-motion: reduce` の場合、演出時間をゼロまたは短縮して最終状態を正しく表示する。

**根拠:** `animateBoardArrival`（5522-5552）、`playWakeCelebrationEffect` / `celebrateClear`（11198-11250）、`setPosition`（7174-7207）、CSS検索語 `welcome-touch-flash`。
**要確認:** 各到着演出の厳密な秒数は再構築で同一でなくてよいが、順番と中間状態の非表示は維持する。

### ASYNC-001 非同期演出と入力ロック

- 盤面には単一の「現在の演出世代」を持たせる。問題変更、モード変更、リロード復元、やり直し、一手戻し、`pointercancel` のいずれかが起きたら、前世代の `requestAnimationFrame`、タイマー、Web Animations の完了処理は現在状態を変更できない。
- 到着、応用導入、自動巻き戻し、通常回転、Undo、Restart、クリアの各状態は相互排他的にし、入力可否を表示状態ではなく状態値から決める。
- 速解きの時計は「壁時計」ではなく、保存済み経過時間＋現在の実行区間で計算する。停止・非表示・問題クリアでは実行区間を一度だけ精算する。
- 現行の世代管理入口は `startBoardAnimationSession` と `uiEffectGenerations` だが、生の `setTimeout` も残る。特に上巻5手時のコツ（7140-7144）、チュートリアル案内（7295-7310）、速解きUndo後の遅延保存（8240-8242）は、再構築では同じ世代管理へ所属させる。

**根拠:** `startBoardAnimationSession/cancelBoardAnimationSession`（5927-5989）、`uiEffectGenerations` / `setUiEffectTimer`（8397-8463）、`scheduleClearFlowDialog`（11279-11286）。

## 1. 共通 UI と進行表示

### UI-001 通常盤面の上部／下部表示

- 通常問題は、現在の手数、残り最短手数、ヒント、やり直し、一手戻す、次へ等を、モードの方針に従い表示する。
- 「だるま学園」の学習区間は、操作のたびに中央に大きく「あと○くるり」を短時間出す。形ガイドを出す局面では数だけの案内を同時に重ねない。
- 学習補助中の表示条件は共通の UI 方針で決め、画面ごとの場当たり判定を作らない。

**根拠:** `currentUiPolicy`、`showMoves`、`showRemaining`（5173-5261）、`showAcademyRemainingCallout`（7161）、`uiPolicy`（1818-1829）。

### UI-002 ダイアログ

- ダイアログは背景を含むモーダルとして一つずつ扱い、節目案内、クリア、コツ、設定、速解き一時停止などを表示する。
- 節目案内は共通の `chainDialog` を内容差し替えで使い、前へ戻る必要がある連鎖では履歴を持つ。
- クリア後の内容（雑学、クイズ、コツ、次へ）はクリアダイアログ内部に表示する。別ダイアログの下に重ねない。
- 画面再読み込み時は、**復元対象と決めたダイアログだけ**を識別子と必要最小限の文脈から再描画する。現行が明示的に再描画するのは、連鎖、クリア、メッセージ、称号一覧、攻略系、速解き一時停止、最短2手一覧／詳細、最短手数失敗などである。
- 単にDOMの `hidden` を解除するだけでは、動的本文、戻り先、フォーカス、アニメーションの実行世代を復元したことにしない。

**根拠:** `captureDialogState` / `persistDialogState` / `DIALOG_RESTORE_HANDLERS` / `restoreDialogState`（4491-4555）、`CHAIN_STEPS` / `openChainedDialog`（6379-6514）、`showClearDialog`（10032）、`renderClearFeatureDialog`（9385）。  
**要確認:** `speedRestart` は保存時の種別名と実DOM IDが一致せず、`twoMoveLessonDialog` などは動的本文を再構築せずDOMを開くだけの経路がある。`OPEN-006` の実機マトリクスで、現行再現か安全な安定画面への復帰かを決める。

### UI-003 表示設定とアクセシビリティ

- 言語、音、盤面テーマ、盤面レイアウト、だるま色を設定でき、設定は保存される。
- 主要な操作はボタンとしてキーボードで到達でき、ダイアログを開いた後は意図した要素へフォーカスする。
- キーボードショートカットがある場合、入力欄・モーダル表示中に誤発火させない。

**根拠:** `STORAGE_KEY_GROUPS.settings`（1533-1538）、`applyLanguage` 検索、`bindMessageReviewEvents`（11947-11983）。
**要確認:** 新版で残す設定項目・ショートカットの一覧は、再構築開始時に現行画面を実見して棚卸しする。

## 2. モードと進行の全体像

### FLOW-001 モード一覧

| モード | 主な役割 | クリアの扱い |
| --- | --- | --- |
| tutorial | 操作を覚える 5 段階 | 自動で次のチュートリアル段階へ |
| stage | 入門、基本、応用、発展、だるま修行 | 進行を記録し通常のクリアフローへ |
| mastery | 名人への道 | 最短手数必須 |
| satori | 悟り | 最短手数必須。二周目は失敗上限が異なる |
| speed | 速解き | 時計・連続出題・専用の一時停止 |
| free | ランダム問題 | 次は別のランダム問題 |
| custom | 自作盤面 | 作成とプレイを切り替える |

**根拠:** `MODES`（1510）、`ACTIVE_MODES`（4450）、`completeBoard`（6973）、`resolveAfterClearRoute`（11346）。

### FLOW-002 コースの順序とゲート

- 本編 `stage` は、入門 3 問 → 基本 9 問 → 応用 9 問 → 発展 8 問 → だるま修行（上・中・下 各9問）の順。
- 一周目でだるま学園を完了すると九番勝負が解放されるが、この時点では修行へ入れない。九番勝負を全9問完走して卒業試験合格フラグが立つと、修行上巻へ進める。
- 修行下巻まで完了すると十八番勝負を修了試験として開始し、全18問完走後に名人への道へ進める。
- 名人への道45問を完了すると二十七番勝負を修了試験として開始し、全27問完走後、かつ名人45問の完了を満たした状態で悟りへ進める。
- 一周目の各試験は最短手数達成数に関係なく、規定問題数の完走で合格する。
- 二周目では九番・十八番・二十七番の試験フラグをゲートに使わない。ただし悟りは、その周の名人45問完了を依然として必要とする。
- 二周目では入門・基本の誘導、応用の紫枠／巻き戻し、発展の候補棒制限は外れる。一方、学園UI自体は `assisted:true` のままで、現在手数等の非表示や学園の残り表示は残る。発展3くるりの形ガイド判定も周回条件を見ていない。

**根拠:** `GATE_BLUEPRINTS` / `canEnter`（1791-1813）、`uiPolicy`（1818-1829）、`pendingSpeedTrial`（5502-5508）、`finishSpeedRun`（5597-5620）、`loadStage/loadExtraStage/loadSatoriStage`（7313-7367）。

### FLOW-003 共通クリアフロー

1. 盤面が全員起きたら、対象モードのクリア記録を確定する。
2. 最短手数必須モードで手数超過なら、成功扱いにせず失敗ダイアログを出す。
3. 通常モードはクリア演出を再生する。
4. 通常問題はクリアダイアログを出し、問題文脈・コツ・クイズ等を表示する。ただし入門、基本、応用、学園、修行、名人の区切り、および悟り最終問題では、通常クリアダイアログを省いて開始／試験／称号等の節目ダイアログへ直接進む場合がある。
5. 通常クリアダイアログでは「次へ」時点で遷移先を確定する。直接表示された節目ダイアログでは、そのダイアログ固有の開始／閉じる操作が次の状態を決める。
6. クリア演出中のリロードやタイマー遅延で、古い予約が新しい画面へ作用してはならない。

**根拠:** `completeBoard`（6973-7006）、`showClearDialog` の区切り分岐（10047-10089）、`CLEAR_FLOW_PHASE` / `CLEAR_FLOW_TRANSITIONS`（11108-11129）、`startClearFlow`、`resolveAfterClearRoute`、`dispatchClearFlowAction`（11253-11395）。

## 3. チュートリアル

### TUT-001 開始と完了

- 初回起動では導入ダイアログ後、チュートリアル未完了ならチュートリアルを始める。
- チュートリアルは 5 問で、1手問題から、場所探し、連続操作、向きまで段階的に教える。
- 各段階をクリアすると短い祝福演出の後に次の段階へ自動遷移する。最終段階を終えると完了を保存し、本編 1 問目を開いて入学案内を出す。
- 再読み込みでは、未完了なら保存済みの段階から再開する。

**根拠:** `TUTORIAL_STEPS`（2118-2123）、`loadTutorialStep/startTutorial/advanceTutorial`（7284-7311）、起動処理（12808-12834）。

### TUT-002 誘導と誤操作

- 段階ごとに、棒をつかむ、場所を探す、連続で回す、方向を見る、といった表示を出す。
- 許可されない棒／方向を回そうとした場合は、正解の盤面状態を変えず自動で戻し、「そこではない」等のフィードバックを出す。
- ヒントの矢印、選択パネル、軸ガイドは段階に応じて使い、次の段階や盤面リセットで必ず消す。

**根拠:** `tutorialMoveAllowed`、`showTutorialCue`（7243-7283）、`finishDrag` の tutorial 分岐（7983-8053）。

## 4. だるま学園（入門・基本・応用・発展）

### ACA-001 入門・基本の補助

- 入門・基本は学習補助あり。最短手数が縮む棒を候補として示し、基本の一部では候補棒を 1〜3 本へ絞る。
- 不正解の棒を選ぶ、または正しい棒を逆方向へ回すと、盤面は自動で元へ戻る。不正解と確定した棒は、その手の間は落下／グレーアウトして再選択できない。
- 正しい棒だが方向だけ違う場合は、棒自体は正解だと分かる状態を保ち、他の不正解候補を落とす。
- 二周目ではこれらの補助を外す。

**根拠:** `uiPolicy`（1820-1829）、`refreshGuidedBasicCandidates`（8679-8699）、`finishDrag` 学園分岐（8003-8102）。

### APP-001 応用クラスの通常問題ルール

- 応用クラスは基本と同じ 2 くるりの 9 問である。
- 各問題には「一回回したあとに揃う三枚」の `targetCells` がある。実盤面ではその三枚を紫枠で示す。
- 目標以外の棒を回した、または目標棒を誤方向へ回した場合、盤面は自動で開始状態へ巻き戻る。誤った棒は落下／候補除外の対象になる。
- 紫枠は対象パネルの子として描かれ、静止・ドラッグ・通常回転・自動巻き戻しのいずれでも対象パネルと一緒に回る。対象三枚は、隣接パネルより前に重なる。
- 紫枠の太さと位置は、パネル境界にほぼ接する外寄せ。寝ている／起きているパネルとの境界線色は、起きているパネル基準で統一する。

**根拠:** `APPLICATION_STAGES`（2355）、`isWrongMoveRewindStage/isApplicationTargetStage`（5188-5191）、`renderApplicationTargetInlineFrame/renderApplicationTargetLayer`（5715-5770）、`startApplicationTargetIntro`（8542）、`finishDrag` application 分岐（8000-8085）。  
**要確認:** 枠の最終ピクセル値は見た目比較で確定する。現行定数は `APPLICATION_TARGET_FRAME_STROKE=7`、`APPLICATION_TARGET_FRAME_OUTSET=1.25`（5705-5714）。

### APP-INTRO-001 応用クラス開始時の完成形見本

**対象:** 補助ありの一周目で、応用クラス各問題を `setPosition()` から読み込むたび。現行には「この問題の見本を既に見た」という永続フラグがないため、初回だけではなく前後移動・再訪・リロード復元でも開始する。

1. 実際の初期盤面や中央から広がる到着演出を先に見せない。実盤面のパネル、水色の棒、金色の軸を隠す。
2. 7枚の静止した完成直前盤面を、簡単にフェードインする。回転アニメーションはしない。
3. 見本は「上2枚＋中央」の3枚が寝た、あと1くるりの状態。三枚を紫枠で示す。
4. 中央の三角を隠さないよう、メッセージ「1回まわして／この形をめざそう」を盤面下方、下の2枚のパネル上に出す。表示時間は読める長さ（現行は約1.8秒、動きを減らす設定では約0.24秒）。
5. 見本とメッセージがフェードアウトし始める**同じタイミング**で、上2枚＋中央の三枚を縮小しながら右上の常設プレビューへ受け渡す。
6. 見本を完全に消した後、実際の初期盤面を中央から広げて表示する。この到着中も、水色の棒と金色の軸を一瞬表示してはならない。
7. 実盤面が到着した後、「あと2くるり」と形名、および寝ただるまの外周をかたどる水色の太い枠を中央に表示する。
8. 形ガイドが消え始めるのと同時に、遅れなく対象三枚の紫枠を表示する。形ガイドだけ消えて紫枠が遅れる状態、または形ガイドが出ない状態は不可。
9. 右上の常設プレビューは、残り手数が 1 以下になるまで表示し、その後フェードアウトする。

- 導入途中の一時段階は保存しない。現行はリロードすると同じ問題を復元した後、導入を先頭から再生する。すでに一手進めた応用盤面を復元する場合も導入開始条件自体は成立するため、再構築でこの挙動を残すかは `OPEN-005` で決める。

**根拠:** `setPosition`（7174-7207）、`startApplicationTargetIntro`（8542-8607）、`APPLICATION_INTRO_TARGET_CELLS=[0,1,3]` / `APPLICATION_INTRO_STATE=[1,1,0,1,0,0,0]`（8540-8541）、`animateBoardArrival`（5522-5552）、`showTrainingShapeCallout`（8272-8316）、`renderApplicationTargetPreview`（8646-8674）。

### APP-INTRO-002 応用開始演出の禁止事項

- フェードイン前に別の盤面アニメーションが見えてから完成形が出ること。
- 見本で三枚以外に紫枠が付くこと。
- メッセージが中央の三角を覆うこと。
- 実盤面到着中に棒6本・軸6個がちらつくこと。
- 水色形ガイドと紫枠の切替に空白時間があること。
- 見本の盤面が「あと1くるり」の正しい静止状態でないこと。

**根拠:** `startApplicationTargetIntro` 内のコメントとタイマー列（8542-8613）。

### DEV-001 発展クラス

- 発展は 3 くるり 5 問と 4 くるり 3 問。学園と同様の補助方針で、最初は候補棒を絞り、間違えた棒は落として再選択できない。
- 前半の3くるり問題で1回正しく回し残り2くるりになった瞬間、修行と同じ形ガイドを出す。単なる「あと2くるり」と重ねず、寝ただるまの台形等をかたどる水色枠も同時に出す。
- 通常の寝→起の途中では、関係ない起きただるまを一瞬フラッシュさせない。クリア時の祝福演出は別。
- 二周目では候補棒の絞り込み・誤手棒の除外は外れる。ただし残り2くるり形ガイドは `isDevelopmentThreeMoveStage()` が周回を条件にしないため、現行では二周目にも出る。

**根拠:** `DEVELOPMENT_THREE_COUNT`（2065）、`isDevelopmentThreeMoveStage`（5183）、`applySwipe` の `shapeCalloutShown`（7146-7150）、`showTrainingShapeCallout`（8272-8316）。

## 5. だるま修行

### TRN-001 構成と開始案内

- 上巻は補助なしの 2 くるり 9 問、中巻は 3 くるり 9 問、下巻は 4 くるり 9 問。
- 各巻に入る前に、登場する形や回し方を紹介する節目ダイアログを出す。盤面を用いたアニメーションを含む案内がある。
- 上巻／中巻／下巻への遷移は、前の範囲を終えた後の節目ダイアログから明示開始する。

**根拠:** 定数（2071-2075, 2389-2414）、`CHAIN_STEPS.trainingUpperGoal/trainingMiddleGoal/trainingLowerGoal`（6335-6520）。

### TRN-002 残り2くるりの形ガイド

- 修行の全巻では、確定操作の結果が残り最短2手で、既知の2手形へ正規化できたとき、盤面中央に「あと2くるり」と形名を表示する。直前の距離は比較していないため、操作後も残り2手なら再表示し得る。
- 同時に、寝ているだるまの連結外周を水色の太い枠で表示する。
- 表示は短時間で自動消去され、次の盤面開始・リセット・別演出で残らない。
- 発展クラス前半の3くるり問題にも同一仕様を適用する。

**根拠:** `isTrainingRangeStage`（5192-5193）、`applySwipe`（7137-7150）、`showTrainingShapeCallout`（8272-8316）。

### TRN-003 上巻のコツとクリア内表示

- 上巻には2くるりの型を説明する「コツを見る」があり、型名、盤面、アニメーション、回し方を表示する。
- 応用クラスの各問題クリア時も、該当する型の内容を**全員起きました！のクリアダイアログ内**に出す。
- 応用クリア時の文面は短くする。見出しは「『{型名}』の回し方」、下側に「しっぽの左右どちらかを回そう」等の回す指示を置く。型の定義説明、選び方のコツ／回し方のコツのタブは出さない。
- 盤面は表示し、アニメーションもする。見出しから回す指示までを枠で囲み、文字を通常より大きくする。

**根拠:** `renderApplicationClearLessonRule`（6708-6737）、`applicationClearLessonContext`（6682-6707）、`openTwoMoveLessonDialog`、`renderClearFeatureDialog`（9385）、検索語 `clearApplicationLessonHint` / `clearTwoMoveLessonRule`。  
**要確認:** 「しっぽ」以外の型の最終文面と、枠の正確な表示範囲は現行画面で目視確認する。

### TRN-004 中巻の開始実演

- 中巻開始案内では、一本の棒を連続して回し、あと3くるり→あと2くるり→あと3くるり→あと2くるりを滑らかに見せる。
- 実演中は、タッチした後に棒を隠し、回転中の三枚・タッチ点・カウンターを見せる。
- 上部の案内文と干渉しない軸を選ぶ。

**根拠:** `buildTrainingMiddleSpinBoard`（6522以降）。

## 6. 速解き

### SPD-001 種別・解放

- 公開される速解きは九番、十八番、二十七番、七十三番。内部標準モードもある。
- 九番は基本の2くるり問題9問、十八番は修行上巻＋中巻18問、二十七番・七十三番は別の問題プールを使う。
- だるま学園を完了した時点で、九番を選べるようにする。九番の初回完走は卒業試験として扱う。
- 九番完走は修行へのゲート、十八番完走は名人への道へのゲート、二十七番完走は悟りへのゲートとなる。試験完走時にはその種別の解放フラグと試験合格フラグを同時に保存する。
- 七十三番は悟り一周目を制覇した時点で解放され、修了試験ではない。

**根拠:** `SPEED_BLUEPRINTS`（1783-1788）、`GATE_BLUEPRINTS`（1791-1795）、`completeBoard` の `unlockSpeedVariant('training9')`（6997-7002）、`finishSpeedRun`（5597-5620）、悟り完了時の `unlockSpeedVariant('satori73')`（8802）。

### SPD-002 セッション開始・時計・連続出題

- 速解きの開始直後は、全員が起きたまっさらな盤面と開始ボタンを表示し、時計を止める。開始操作後に第1問を到着演出で出し、時計を動かす。
- 問題順は種別ごとに生成・保存する。九番は毎問、対称変換により向き／表裏を変える。
- 各問題を解くと通常のクリアダイアログは出さず、短いクリア演出の後に次問へ自動で進む。最終問を終えると記録・完走ダイアログへ進む。
- 最短手数で解けた問題数、完走時間、上位記録を保存・表示する。
- 試験として開始した九番・十八番・二十七番の最終問後は、汎用の速解き完走ダイアログではなく、対応コースの合格／称号ダイアログへ合流する。通常のメニュー起動なら速解き完走ダイアログを出す。

**根拠:** `newSpeedSession/speedAwaitingStart`（5510-5513）、`loadSpeedStage/beginSpeedRun/advanceSpeedRun/completeSpeedStage`（5553-5666）、`makeSpeedOrder`（5400-5411）、`speedStatsData`（5622-5628）。

### SPD-003 速解き九番勝負の盤面UI

- 盤面上部のUIは、だるま学園の共通表示方式に合わせる。
- タイマーは必須で、タイマーの**左**に大きく「あと○くるりでクリア」を置く。位置は一時停止ボタンと上下中央が揃う程度に、タイマーを少し右・少し下へ寄せる。
- 「いま○くるり」、やり直す、一手戻す、次の1手、コツを見るは表示しない。
- 「コツを見る」があった位置には一時停止ボタンを置く。一時停止ボタンは元のコツを見るボタンと同じ大きさ。
- 九番は正解候補を最低1本含め、1〜3問目は3本、4〜6問目は4本、7〜9問目は5本を表示する。間違えた棒はその問題の現在状態で落として再選択できない。
- モード定義上は `allowsUndo:true` だが、九番は `assisted:true` でもあり、現行表示では「やり直す」「一手戻す」の両ボタンを隠す。再構築の観測仕様は**ボタンを出さない**を正とし、内部フラグを理由に一手戻しUIを復活させない。

**根拠:** `SPEED_BLUEPRINTS.training9`（1785）、`speedShowsRemaining/speedAllowsUndo`（5293-5294）、`computeSpeedTrainingCandidateTis`（8514-8527）、`renderStageNavControls`（10900-10945）、`showRemaining`（5251-5261）、速解きDOM検索語 `speedPause`, `speedClockValue`, `speedBoardStart`。
**要確認:** 上部UIの最終座標・余白はCSS目視比較で確定する。

### SPD-004 一時停止・再開・再読み込み

- 一時停止を押すと時計を止め、現在の問題番号、経過時間、記録を含む一時停止ダイアログを出す。
- 一時停止から再開すると、ユーザー操作後に時計を再開する。再スタートは確認ダイアログを経由し、新規セッションへ置き換える。
- `pagehide` とタブ非表示時は時計を止めてセッションを保存する。
- タブ復帰時、時計を黙って再開しない。クリア済みで停止したタイマーがあればまず次問へ確定し、そうでなければ一時停止ダイアログを出す。
- 初回クリア前にリロードしても、速解き以外の通常問題へ戻してはならない。最後に遊んだモードが速解きなら、保存した速解き種別・問題順・盤面・時計を復元する。
- 通常のページ再読み込みは「非表示から同じJavaScript実行環境へ戻る」場合と異なる。現行は、開始済みセッションを復元すると `loadSpeedStage()` の末尾で時計を自動再開する。一時停止ダイアログ自体が保存されていた場合は、その後のダイアログ復元で再び停止する。
- スタート待ちで再読み込みした場合は、全員起きた盤面と開始ボタンへ戻り、時計は開始しない。クリア直後から次問へ進む前に再読み込みした場合は、保存盤面が解決済みなら同じ問題を重ねず即時に次問／完走へ進める。

**根拠:** `openSpeedPauseDialog`（5476-5483）、`speedAwaitingStart`（5512-5513）、`loadSpeedStage`（5553-5579）、`restoreActiveSession`（7458-7474）、起動時 `restoreActiveSession()`（12810）、`pagehide` / `visibilitychange`（12820-12834）、`bindSpeedEvents`（11915-11945）。
**要確認:** 新版でも「通常リロード後は即時計再開」を採用するか、一時停止確認へ統一するかは `OPEN-007` で決める。

## 7. 名人への道・悟り・自由・自作

### MST-001 名人への道と悟りの制約

- 名人への道と悟りでは、最短手数で解かなければクリアにならない。超過時は失敗ダイアログから再挑戦する。
- 悟り二周目は残り最短手数を隠し、成功しない限り4手目まで操作できる。
- 第四巻相当の範囲では、残り手数を見る回数に制限があり、条件によりやり直し・一手戻しでも使用回数を戻さない。

**根拠:** `requiresOptimalClear`（5197）、`completeBoard`（6979-6980）、`paint` の satori 分岐（7042-7049）、`fourthCheckLimit`（5203-5242）。

### FREE-001 自由問題

- 自由モード開始時、距離2〜4の問題からランダムに選ぶ。直前と同じ初期状態は可能なら避ける。
- 自由モードを表示したままリロードした場合は、途中盤面・履歴を `activeSession` から復元する。同じページ内で他モードへ出て戻る場合は、メモリ上の `savedFreeSession` から復帰する。現行の `savedFreeSession` は統合保存の `gameState.ui` に含まれないため、**自由から別モードへ出た後にリロードしてから自由へ戻る**ケースまで同じ盤面を保証してはいない。
- クリア後の次は別の自由問題。

**根拠:** `startFree`、`persistFreeSession`、`restoreFreeSession`、`leaveFreeMode`（7498-7550）、`resolveAfterClearRoute`（11346-11360）。

### CUSTOM-001 自作盤面

- 自作モードに入ると、全員が起きたまっさらな盤面から編集する。
- 現在の盤面について、解けるか／最短何手かを表示する。解けない、または既にクリアならプレイ開始できない。
- プレイ開始後は、その自作状態を初期盤面として通常操作する。

**根拠:** `enterBoardMaker`、`showMakerMessage`、`playCustomBoard`、`resetMakerBoard`（7555-7586）。

## 8. 保存・再開・リセット

### PERSIST-001 保存対象

- 保存するもの: 設定、進行済み問題、周回、解放・報酬、現在モードと問題番号、盤面の向き・配置・手数・履歴・初期状態、速解きセッション、および復元対象として明示したダイアログ状態。
- 盤面保存値は妥当性検証後にだけ復元する。壊れた値、到達できない進行先、未解放モードは安全なステージへフォールバックする。
- 保存はページ非表示時にも行う。
- 応用導入の途中段階、形ガイドのフェード位置、到着／回転アニメーションの途中フレーム、通常の `busy` は保存しない。復元時は同じ問題の定義済み安定状態へ正規化する。

**根拠:** `WakeSevenState`（1494-1773）、`serializeActiveBoard/validSavedBoard/restoreSavedBoard`（7387-7430）、`persistActiveSession/restoreActiveSession`（7432-7497）。

### PERSIST-002 リロード復帰の優先順位

1. 未完了チュートリアルを最後に遊んでいたなら、その段階を復帰する。
2. 最後に遊んでいたモードが速解きで、有効な未完走速解きセッションがあるなら、それを復帰する。
3. それ以外の有効な保存盤面があれば、その stage/mastery/satori/free/custom 状態を復帰する。
4. どれも無効なら、保存された現在ステージ、さらに不可能ならステージ0へ戻る。
5. 表示中だった復元可能なダイアログを最後に再構築する。

- 速解きの保存セッションが残っているだけでは、速解きへ戻さない。`activeSession.mode === 'speed'`、かつ有効な未完走セッションがあることが必要である。
- 解決済み盤面は `restoreSavedBoard()` で `clearShown=true` に正規化される。通常問題では保存済みクリアダイアログを戻せるが、クリア演出の途中時刻そのものは復元しない。

**根拠:** `restoreActiveSession`（7458-7497）、`restoreCurrentStage`（7372-7385）、`restoreDialogState`（4537-4555）、起動処理（12808-12834）。

### PERSIST-003 リセット

- 通常リセットは進行、二周目状態、覚醒状態、メッセージ閲覧状態を消す。金盤面・悟り盤面・虹だるま・3Dなどの既獲得報酬、速解き解放／記録、設定は保持する。
- 完全リセットは設定、報酬、速解き記録と解放も消す。
- リセット中の速解き時計は停止する。完了後はステージ0を開き、指定時のみ導入を再表示する。

**根拠:** `resetStoredProgress`（12711-12787）、設定ダイアログの各イベント（12789-12798）。

## 9. 再構築時に判断を保留する事項

- **OPEN-001:** 現行の全ダイアログ文言・多言語文言・全コツカードを、同じ範囲で移植するか。今回は機能仕様として必要なものを優先し、文章そのものは別途棚卸しする。
- **OPEN-002:** 現行CSSのピクセル単位の余白・色・影・アニメーション曲線を完全一致させるか。ゲーム性に関わるレイアウト（棒、メッセージ、紫枠、速解き上部UI）は一致対象、装飾は要確認。
- **OPEN-003:** `all-patterns.html` と `index_3D.html` をV2の同時対象にするか。本台帳は `index.html` のみを対象とする。
- **OPEN-004:** 既存の旧 `localStorage` キーを新実装で消すか残すか。互換不要前提では新しい名前空間にして旧値は無視する。
- **OPEN-005:** 応用導入を「一周目で問題を開くたび」に再生するか、「各問題の初回だけ」にするか。さらに、一手以上進んだ盤面をリロード復元したときも先頭から再生するかを決める。確認方法: 一周目の同じ応用問題へ前後移動で再訪し、0手／1手のそれぞれでリロードして、見本・復元盤面・右上プレビューの順を録画する。
- **OPEN-006:** ダイアログごとのリロード契約を決める。確認対象: `chainDialog`, `clearDialog`, `messageDialog`, `masterDialog`, `speedPauseDialog`, `speedRestartDialog`, `twoMoveDialog`, `twoMoveDetailDialog`, `twoMoveLessonDialog`, `optimalFailDialog`, 設定系。各ダイアログを開いてリロードし、本文、ボタン、戻り先、フォーカス、盤面アニメーションを表に記録する。現行の壊れた復元まで再現する必要はない。
- **OPEN-007:** 進行中の速解きを通常リロードした直後、時計を即時再開するか、常に一時停止ダイアログを介するか。現行は前者、タブ非表示からの復帰は後者である。確認方法: 同じ問題・同じ経過秒で F5 とタブ切替を別々に行い比較する。
- **OPEN-008:** 自由モードを離れた状態でリロードした後も、以前の自由盤面へ戻れることを新版の契約に含めるか。含めるなら `savedFreeSession` 相当も永続化する。
