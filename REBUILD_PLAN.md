# Wake Seven 単一HTMLリビルド計画

> 対象: `index.html`、`REBUILD_SPEC.md`、`REBUILD_CHECKLIST.md`（2026-09-06 時点）
> 基準点: commit `be03b84` / tag `rebuild-baseline-2026-09-06`
> 原則: README は参照しない。現行 `index.html` は動く仕様書として凍結し、手編集する新しいHTMLをゼロから縦に組み上げる。

## 1. 成功条件

リビルドは、次をすべて満たした時だけ完了とする。

- 新版は外部ビルド、生成スクリプト、実行時のローカル依存なしで、単独の `index.html` として起動する。開発中の正本もHTML 1枚であり、CSS・DOM・JavaScriptを別ソースへ分割しない。
- `REBUILD_SPEC.md` の全仕様IDについて、`REBUILD_CHECKLIST.md` の「実装」「PC確認」「スマホ確認」「リロード確認」が完了している。
- 七枚・3値の全 `3^7 = 2187` 状態について、符号化、遷移、逆操作、解決状態からの到達可能性、到達可能状態の最短距離、ステージ定義の整合性を確認できる。
- PC、360px級スマホ、マウス、タッチ、キーボード、通常表示、reduced motion、リロード、タブ非表示復帰を通して、盤面・履歴・進行・時計・ダイアログが混線しない。
- 応用導入、紫枠、残り2手形ガイド、速解き九番UI、試験ゲート、クリア後分岐は、現行版との並列比較で合意した観測結果に一致する。
- 問題／モード切替後に旧世代のタイマー、`requestAnimationFrame`、Web Animations完了処理が状態やDOMを変更しない。
- 保存値が正常なら定義済みの安定状態へ復帰し、破損・旧版・未解放の値なら安全にフォールバックする。
- 新HTML内は自然な機能名と命名規約を守り、1機能の状態、ポリシー、描画、操作入口、演出を必要に応じた `rg` で辿れる。
- 切替直前まで基準版 `index.html` を変更せず、切替後もタグまたはrevertで即時に戻せる。

### 非目標

- 既存JavaScriptの段階的リファクタリング、既存関数の延命、既存グローバルAPIとの内部互換は行わない。
- 旧 `localStorage` の移行は、`OPEN-004` で明示的に覆さない限り行わない。
- アニメーション途中のフレームやドラッグ途中を保存・復元しない。安定状態への正規化を契約とする。
- `OPEN-002` で一致対象にしない装飾のピクセル完全一致、未使用コード、内部標準速解きの公開は必須にしない。
- `all-patterns.html` と `index_3D.html` は `OPEN-003` で対象化されない限り改修しない。
- 新機能追加、ゲームルール変更、大規模なデザイン刷新を移植と同時に行わない。
- 日付ベースの巨大な見積もりを作らない。進捗はフェーズの観測可能な完了条件で判断する。

### AI改修を最優先する契約

この再構築の第一目的は、**AIが機能追加・故障改修を、速く、正しく、低い探索コストで行えること**である。見栄えのよい分割、一般的な設計様式、将来使うかもしれない抽象化は、この目的を上回らない。

- 改修調査はユーザーの依頼文だけを入口とし、AI自身が自然に検索語を決める。実装者が指定した専用キーや仕様IDを知っていることを前提にしない。
- AIが日本語、英語の実装語、UI文言、DOM ID、CSSクラスのどこから調べ始めても、主実装へ到達できる名前と配置にする。特定の言語や一回の検索成功だけを合格条件にしない。
- 主たるJavaScript実装は機能単位で連続して置き、検索で着いた周辺から、目的、状態の所有者、入力、状態変更、描画、演出、検証との関係を説明できるようにする。データ、描画、イベント、検証を「整理のため」だけに別の汎用セクションへ散らさない。
- 検索専用の記号、キーワード一覧、空の `START/END`、対象なし表、将来用の抽象APIは `index.rebuild.html` に置かない。コメントはその場所の処理目的や判断理由を説明する場合だけ書く。
- 共通化は、少なくとも二つの**実装済み**機能が同じ入出力契約で使う処理に限る。共通化によって検索結果から機能の流れが読めなくなるなら、重複を選ぶ。
- 各フェーズの完了前に、実装者とは別のレビュー文脈で、関数名や検索語を教えずユーザー風の改修依頼だけを渡す。レビューAIが自分で `rg` し、変更箇所、目的、影響範囲、関連検証を説明できるかで判断する。使用した検索語と到達箇所は完了報告に記録するが、HTMLへ転記しない。

## 2. ファイル、ブランチ、バックアップ

### 開発中

1. `rebuild-baseline-2026-09-06` から `rebuild/single-html-v2` ブランチを作る。
2. 現行 `index.html`、`REBUILD_SPEC.md`、`REBUILD_CHECKLIST.md` は比較・受入れ資料として凍結する。仕様判断を記録する必要が出た場合も、実装タスクと混ぜず、明示された文書更新タスクに分ける。
3. 新版はリポジトリ直下の `index.rebuild.html` として、最小のHTML骨格から作る。現行HTML全体の複製を開始点にしない。
4. `index.rebuild.html` が唯一の新版ソースである。別CSS、別JS、生成元ディレクトリ、バンドラー、インライン化スクリプトを作らない。
5. 各フェーズは前フェーズの受入れ後に1コミットとする。コミット例は `rebuild(p1): complete board domain`。未完の横断改修を複数フェーズへまたがせない。
6. 盤面基盤完了時と全機能RC時に、必要なら注釈付きタグ `rebuild-v2-core`、`rebuild-v2-rc1` を打つ。基準版の復旧点は既存タグを正とし、日付付きコピーHTMLを増やさない。

### 凍結ルールと停止条件

- 各フェーズ開始時に `git status --short` と現在ブランチを確認する。意図しない変更、特に凍結ファイルの差分があれば作業を止める。
- `git diff -- index.html REBUILD_SPEC.md REBUILD_CHECKLIST.md` が空でない状態では、切替フェーズ以外を完了にしない。
- 前フェーズの完了条件、ドメイン自己検査、対象の重点シナリオに失敗が残る場合、次フェーズへ進まない。
- 対象フェーズに必要な `OPEN-*` が未決なら実装で推測せず、そのフェーズを止めて決定を記録する。
- コミット直前に `git diff --check`、HTMLの起動、対象シナリオを確認する。生成物や一時キャプチャはコミット対象に混ぜない。

## 3. 新HTMLの内部構成

HTMLの並び順は、画面に近い順にする。CSSは共通見た目→盤面→各画面固有、DOMは画面本体→ダイアログ→SVG部品、JavaScriptは問題データ→盤面ルール→状態→描画→入力→その画面の操作→自己検査→起動、を基本にする。各機能は、必要な状態、表示、操作、検査をできるだけ近接配置する。

コメントは「棒の当たり判定」「クリア演出」「速解きの時計」のように、コードの意図を説明するためだけに書く。`WAKE7:`のような検索専用の記号、仕様IDの反復、空のセクション見出しはHTMLへ置かない。

### 改修時の調べ方

- AIは依頼文の自然な語、日本語UI文言、関数名、DOM ID、CSSクラスのいずれからでも必要に応じて `rg` する。特定の検索語や一回の検索成功を契約にしない。
- 「棒の判定」「回転」「クリア」「一手戻す」「保存」のような自然な依頼から調べた時、該当する説明コメント、ユーザー向け文言、または自然な関数名を入口に、関連処理を近い範囲で読めることを受入れ条件にする。
- 日本語のコメントだけ、英語の関数名だけのどちらかに依存しない。日本語の依頼文と、`hitTestGrip`、`runClearEffect`、`intentUndo` のような実装語の双方から辿れるようにする。
- 共通基盤へ出してよいのは、2機能以上が同じ契約で利用する処理だけとする。共通化した機能からは名前付きAPIを呼び、共通セクションに機能固有の分岐を蓄積しない。

### 命名規約

- JavaScript: 定数は `UPPER_SNAKE_CASE`、関数と値は `camelCase`、概念を表す固定オブジェクトは単数名とする。
- DOM参照: IDは `w7-<feature>-<role>`、`data-w7-action` はユーザー操作、`data-w7-ref` は描画参照に使う。ID文字列を機能コードへ散在させず、DOM参照を使う描画・操作処理の近くで一度だけ収集する。
- CSS: `.w7-<block>__<element>` と `.is-*` を使用する。`.is-*` は描画結果であり、ゲーム状態の正本にしない。`hidden`、`opacity:0`、要素の有無を入力可否判定に使わない。
- 状態遷移: ユーザー意図は `intent*`、検証済みの同期更新は `commit*`、派生値は `select*`、描画は `render*`、演出開始は `run*Effect`、保存は `save*` / `restore*` とする。
- イベントハンドラは `handle<対象><イベント>` とし、DOMイベントから意図へ変換するだけにする。
- 問題は配列添字だけで永続化せず、安定した `puzzleId` とカタログ版を持つ。画面上の番号と内部IDを分離する。
- マジック数値は対象セクション冒頭へ名前付きで置く。演出時間は reduced motion 値と対で定義する。

## 4. 状態所有と依存方向

### 永続AppState

```text
AppState
├─ schemaVersion / catalogVersion
├─ navigation: mode, lap, puzzleId, mode別index, returnRoute
├─ board: initialCells, cells, history, view, eliminatedRods, clearCommitted
├─ progress: lap1/lap2のstage・mastery・satori完了
├─ unlocks: コース・速解き等の利用可否
├─ rewards: masterGoldGranted, satoriDesignGranted等の獲得済み状態
├─ tutorial: completed, step
├─ speed: variant別session, best, history, trial状態
├─ settings: language, sound, theme, layout, themeChosen, layoutChosen, darumaColor, introSeen
└─ ui: 復元対象dialog descriptor, clear-flow checkpoint
```

- `cells` は長さ7、各値0〜2の正規盤面だけを保持する。ドラッグ中の角度は含めない。
- `history` は確定手だけを持ち、`moveCount` は原則 `history.length` から導出する。誤手の自動巻き戻しを履歴へ積まない。
- 盤面の論理状態と見た目の回転・反転 `view` を分ける。
- `rewards` は「選べる資格」、`settings` は「現在の選択」を所有する。同じフラグで兼用せず、未獲得のテーマ・配置が保存されていた場合は既定表示へフォールバックする。
- `themeChosen` / `layoutChosen` は、報酬獲得時の初回自動適用と、ユーザーが後から明示的に選んだ設定を区別する。盤面報酬の表示と切替はP6〜P8で実装し、P3では保存スキーマと既定値だけを確定する。
- `dialog descriptor` はダイアログ種別と再描画に必要な最小文脈だけを持ち、生成済みHTMLを保存しない。
- 速解きは `awaitingStart | running | paused | transitioning | completed` を明示し、経過時間は `elapsedCommittedMs` と実行区間から求める。実行区間開始時刻は永続値にしない。

### 非永続RuntimeState

```text
RuntimeState
├─ activity: idle / dragging / arriving / rotating / rewinding /
│            undoing / restarting / celebrating / applicationIntro
├─ effectGeneration / AbortController
├─ activePointer / dragPreview
├─ animation・timer・requestAnimationFrameの登録集合
├─ speedSegmentStartedAt
└─ focusReturnElement
```

問題変更、モード変更、復元、Undo、Restart、`pointercancel` は最初に世代を更新して旧処理を中止する。`canAcceptBoardInput(state, runtime)` を唯一の盤面入力ゲートとし、`activity === idle`、モーダルなし、未クリアなどを明示的に検査する。

### 一方向の依存

```text
静的データ ─┬→ 純粋盤面ドメイン ─→ コマンド／状態遷移 ─→ AppState
             └→ セレクタ／進行ポリシー ──────────────┘
AppState + セレクタ ─→ 描画 ─→ DOM
DOMイベント ─→ intent ─→ コマンド
コマンド ─→ Effect Runner ─→ 完了intent（世代一致時のみ）
AppState ⇄ 保存アダプタ（起動・確定点・pagehideのみ）
```

禁止する依存は次の通り。

- 盤面ドメインはDOM、言語、時刻、保存、モードへ依存しない。
- 描画は状態を変更せず、保存せず、タイマーを開始しない。
- DOMイベントは盤面配列、進行フラグ、DOM表示を直接変更しない。
- 演出完了処理は直接永続状態を書かず、世代付きの完了intentを返す。
- 保存層はDOMを読み取らない。復元時の妥当性検査はスキーマ、カタログ、盤面ドメインに限定する。
- ポリシーはDOMの表示状態から判断しない。モード、周回、問題、進行から表示モデルと許可操作を返す。

## 5. 保存契約

- 新版は旧版と衝突しない単一名前空間（仮: `wake7-rebuild-v1`）を使う。正式名は `OPEN-004` でP3開始前に固定する。
- 保存形式には `schemaVersion` と `catalogVersion` を必須とする。旧版キーを列挙削除しない。
- コマンドが安定状態を確定した直後、速解きを停止した時、`visibilitychange:hidden`、`pagehide` で保存する。`pointermove` やアニメーションの各フレームでは保存しない。
- 復元は `PERSIST-002` の優先順位を1関数に実装する。復元対象外の演出状態は捨て、同じ問題の安定状態へ正規化してから描画する。
- 盤面は型、長さ、値域、問題ID、初期盤面、履歴を検査し、履歴の再生結果と保存盤面が一致しなければ破棄する。
- 進行先と解放状態はゲートポリシーで再検査する。破損した一領域のために全設定まで消さず、領域単位でフォールバックする。
- ダイアログは `OPEN-006` の許可リストだけ復元する。本文、操作、戻り先、初期フォーカスをdescriptorから再描画できないものは閉じた安定画面へ戻す。
- 通常リセットと完全リセットは削除対象領域を宣言表で分け、速解き時計を先に停止する。リセット処理から旧名前空間へ触れない。

## 6. 縦の実装フェーズ

各フェーズは「入力」を必要箇所だけ読み、「作業」を `index.rebuild.html` 内で完結させ、「完了条件」と「検証」を満たしてから記載のコミット境界で止める。

### P0. 契約凍結と比較準備

- **入力:** 全仕様ID、チェックリストA〜E、基準タグ、`OPEN-001`〜`OPEN-008`。
- **作業:** ブランチと候補ファイル名を固定する。現行版のPC/360px基準画面、主要状態、設定項目、ダイアログ一覧を記録する。`OPEN-003` を決め、`OPEN-001`・`002`・`006` の棚卸し表を作る。決定は文書更新が許可された別タスクで仕様台帳へ反映する。
- **完了条件:** 比較するURL、保存名前空間、viewport、言語、テーマ、テスト開始状態が固定され、凍結ファイルに差分がない。
- **検証:** 基準タグを別checkoutまたはブラウザで開け、現行デバッグ導線で重点状態へ到達できる。
- **コミット境界:** `rebuild(p0): add clean single-html shell`。メタ情報、P1に必要な最小DOM、起動失敗表示まで。将来機能の空DOMや検索専用コメントは置かない。
- **停止:** `OPEN-003` が未決、比較用現行版が起動しない、基準タグが参照できない場合。

### P1. 純粋盤面ドメインと静的盤面

- **仕様:** `BOARD-001`。
- **入力:** 現行の盤面ジオメトリ、`WakeSevenBoardDomain`、三角定義、向きの見た目。文言やモード処理は読まない。
- **作業:** 7セル、3値、6本の三角軸、符号化／復号、±120度のroll、全探索ソルバを純粋関数で実装する。1問を静的SVGへ描画する。盤面の論理向きとview変換を分離する。
- **完了条件:** 自己検査が2187状態を完走し、静的な初期盤面と解決盤面を描ける。盤面機能は自然な関数名、DOM名、説明コメントから実コードとして辿れる。未実装の7機能について、検索専用コメントや画面UIを置かない。
- **検証:** 7章の全状態検証を実施し、PC/360pxで現行とセル位置・寝方・軸位置を比較する。
- **コミット境界:** `rebuild(p1): implement board domain and static renderer`。
- **停止:** 到達可能性の分類、逆操作、ステージ `par` のいずれかが参照計算と一致しない場合。

### P2. 盤面操作、履歴、排他的演出

- **仕様:** `BOARD-002`、`BOARD-003`、`BOARD-004`、`ASYNC-001`。
- **入力:** P1、現行のpointer処理、到着・回転・Undo・Restart・クリア演出の観測結果。
- **作業:** RuntimeState、入力ゲート、pointer capture、ドラッグpreview、確定／cancel、Undo、Restart、Effect Runnerを実装する。全タイマー、RAF、Web Animationsを世代へ登録する。
- **完了条件:** 静的な1問をマウス／タッチで解き、戻し、やり直せる。状態変更中の連打が無効で、reduced motionでも最終状態へ到達する。
- **検証:** pointercancel、盤外release、短いdrag、逆方向、連打、Undo中のRestart、到着中tap、クリア中tapを実施する。状態、履歴、DOMの向きが常に一致する。
- **コミット境界:** `rebuild(p2): add transactional board interaction and effects`。
- **停止:** 入力可否がCSS表示に依存する、未登録の生タイマーがある、演出から直接永続状態を変更する経路がある場合。

### P3. 最小通常問題、クリアフロー、保存復元

- **仕様:** `FLOW-001`、`FLOW-003`、`UI-001`、`UI-002` のクリアダイアログ部分、`PERSIST-001`、`PERSIST-002`、`REWARD-001` の保存構造部分。
- **入力:** P2、通常問題1〜2問、クリアフローの分岐表、`OPEN-004`、クリアダイアログに関する `OPEN-006` 決定。
- **作業:** AppState、コマンド、セレクタ、最小ナビ／メトリクス、クリア確定、演出、ダイアログ、次問題を一本の流れにする。新名前空間へ保存し、起動時優先順位と破損値フォールバックを実装する。将来の報酬について、獲得済み状態 `rewards` と現在選択 `settings`、明示選択フラグを別領域として定義する。この段階では金色・悟り盤面の表示、獲得、設定UIは実装しない。
- **完了条件:** 開始→操作→クリア→ダイアログ→次問題が通り、0手、1手、複数手、Undo後、クリア確定後のリロードが定義済み状態へ戻る。報酬・設定領域は既定値で保存復元でき、未獲得報酬を選択した矛盾値は既定表示へ正規化される。
- **検証:** 保存JSONの各領域を欠落・型違い・範囲外へ変え、安全な復元を確認する。報酬未獲得のまま金色／悟りテーマまたは30度傾斜を保存したケースも確認する。クリア直後の連続リロードで二重記録・二重遷移がない。
- **コミット境界:** `rebuild(p3): complete minimal stage and reload flow`。
- **停止:** `OPEN-004` または必要なクリアダイアログ復元契約が未決、解決済み盤面の復元規則が一意でない場合。

### P4a. 共通進行入口とチュートリアル

- **仕様:** `TUT-001`、`TUT-002`、`PERSIST-001`、`PERSIST-002` のチュートリアル文脈、`FLOW-003` のチュートリアル完了分。
- **入力:** P3、チュートリアル5段階、初回導入と入学案内の観測結果、入門1問目の静的データ。
- **作業:** 現在の盤面、入力、回転、Effect Runner、保存復元をそのまま共用する。初回導入→5段階→入学案内までを一本道で実装し、段階ごとの正解棒・方向・案内だけをチュートリアルデータの近くへ置く。許可されない棒／方向は履歴へ確定せず、既存の回転復帰で戻す。P3の仮の通常問題を入門データと誤認して引き継がない。**進行だけを先に通して完了扱いにしない。** 旧版を挙動・見た目の基準として、導入ダイアログの七枚見本、上側の案内位置、段階ごとの棒の絞り込み／全棒表示、正しい棒をつかんだ後の文言・方向弧、離す位置の文言変更、誤棒の暗転と復帰、誤方向の巻き戻し、クリア文言と自動遷移までを同じ共有盤面上で移す。
- **完了条件:** 新規状態で導入から5段階を完走し、最後に入門1問目と入学案内へ着く。チュートリアルの0手、2手問題の1手後、各段階クリア直後、最終遷移後のリロードが安定状態へ復元する。前段階の棒強調・案内は次段階に残らない。加えて、上記のチュートリアル固有UI／反応を旧 `index.html` と並べて確認し、段階ごとの差分を「新実装だから」で残さない。
- **検証:** チェックリストC5・C8のチュートリアル対象を実施する。各段階の静的state、par、明示した正解手順を自己検査し、誤棒・逆方向が盤面履歴を変えないことを確認する。PCとスマホで同じ `#w7-board` を使う。
- **コミット境界:** `rebuild(p4a): add shared tutorial and course entry flow`。
- **停止:** チュートリアル用の盤面DOM／回転実装を複製する必要が出た、保存済みの段階と盤面履歴を一意に復元できない、旧版との差分理由を説明できない場合。

### P4b. 入門・基本クラス

- **仕様:** `ACA-001`、`UI-001` の学園部分、`FLOW-003` の入門／基本遷移部分、`PERSIST-001`・`PERSIST-002` の通常ステージ文脈。
- **入力:** P4a、入門3問 `31 / 558 / 837`、基本9問 `922 / 1516 / 526 / 544 / 1732 / 196 / 76 / 7 / 57`、旧版の入学→入門開始・基本開始デモ、候補棒と誤手の観測結果。
- **範囲:** 一周目の入門・基本を完成させる。二周目の補助解除、応用の実盤面・紫枠・開始演出は後続フェーズとし、P4bでは基本9問目クリア後の「応用クラス開始待ち」という安定境界までを所有する。
- **問題カタログ:** `academy-intro-001`〜`003` と `academy-basic-001`〜`009` を学習順に固定し、`classId`、クラス内番号、`state`、`par`、初期候補棒数、必要な表示例外を同じ定義に置く。基本の初期候補数は1問目=1本、2問目=2本、3〜9問目=3本、残り1くるりでは6本とする。旧版の基本6問目で右上の正解棒を優先する例外は、問題データの明示フィールドにする。
- **進行と画面:** P4aの入学案内から、入門開始デモ→入門3問→基本開始デモ→基本9問→応用開始待ちを一方向に結ぶ。開始デモは入力不能な見本盤面で「あと1→0」「あと2→1」と棒を離すタイミングを見せる。実問題は承認済みの七枚同時到着を使い、クラス名・問題番号・残り最短手数を表示する一方、学習区間では現在手数、Undo、Restart、ヒント、盤面変形操作を表示しない。
- **候補棒:** 現在の確定盤面から「一手で最短距離が1減る棒」を計算し、少なくとも1本を候補へ含める。基本では問題データの本数まで誤答候補を加える。候補集合は `puzzleId + encodedCells + history.length` から決定的に導出し、同じ安定盤面の再描画・再読込で勝手に入れ替わらないようにする。候補、非候補、落下済みの入力可否はCSSではなくセレクタで決める。
- **誤操作:** 不正解棒なら「そこじゃないよ」を出して離した角度から自動巻き戻しし、その棒を同じ手の間は落下／グレーアウトして入力不可にする。正解棒の逆方向なら「そっちじゃないよ」を出して巻き戻し、選んだ棒は残し、それ以外の不正解候補を落とす。120度以上回して0度へ戻した往復も棒選択の判定対象にする。240度等は共有の実効方向（`+240 = -120`、`360 = 0`）で判定し、非ゼロの確定は見た目の周回数にかかわらず1くるり・履歴1件とする。
- **正解操作:** 正しい一手だけを履歴へ確定し、盤面・残り手数・候補集合を同時に次状態へ更新する。未クリアなら盤面中央に大きく「あと○くるり」を短時間表示し、通常の上部残り表示と役割を混ぜない。候補の落下状態は一手確定時、Restart、問題変更で破棄する。
- **クリアと境界:** 通常問題はクリア確定→共通祝福演出→復元可能なクリアダイアログ→「次へ」で一度だけ進む。入門3問目は通常クリアダイアログを省いて基本開始デモへ、基本9問目は応用開始待ちdescriptorへ進む。入門・基本に属する既存の短いコツ／雑学は問題データ近傍の表示データとして移し、本文の有無を進行分岐に使わない。
- **一時状態と保存:** 候補から落ちた棒、巻き戻し角度、中央の残り表示、開始デモの途中フレームは保存しない。リロード時は最後に確定した盤面・履歴から候補集合を再構築する。クリア演出中は次の問題またはクラス境界へ、クリアダイアログ中は同じdescriptorへ正規化し、二重完了・二重遷移を起こさない。
- **実装順:** (1) 12問カタログとroute正規化、(2) 学園UIポリシー、(3) 候補棒セレクタと入力ゲート、(4) 誤手巻き戻し・落下・中央残り表示、(5) 開始デモ・クリアダイアログ・クラス境界、(6) 自己検査と実機比較、の順に縦に通す。各機能は `index.rebuild.html` の入門・基本ブロックへ寄せ、AIが依頼文から自然に選んだ検索語で実装と意図へ到達できるようにする。
- **完了条件:** 入学案内から入門3問・基本9問を正解、誤棒、逆方向、240度相当操作を含めて進み、基本9問目後の応用開始待ちまで到達する。各問題の0手・1手後・クリア確定後・クリアダイアログ中、入門→基本境界、基本→応用境界のリロードが定義済み安定状態へ戻る。候補本数、落下、中央の残り表示、ボタン非表示、開始デモが旧版と一致し、承認済み到着演出以外の未説明差分がない。
- **検証:** 自己検査で12問の順序・最短距離、候補数、候補内に正解棒があること、実効方向、誤手でcells/historyが変わらないこと、正解で距離が1減ること、両クラス境界を確認する。手動では入門1/3、基本1/2/3/6/9をPCと360pxで確認し、各点で誤棒、逆方向、往復、240度、途中リロード、巻き戻し中の非表示復帰、クリア中の非表示復帰を行う。E2Eは作らない。
- **コミット境界:** `rebuild(p4b): add intro and basic academy guidance`。
- **停止:** 学園補助が盤面ルールや通常の入力経路を複製する、候補棒と誤手の状態所有がDOMと状態に二重化する、同じ保存盤面から候補集合が一意に再構築できない、クラス境界をダイアログの表示有無だけで判定する場合。

### P5. 応用クラス（4段階）

- **仕様:** `APP-001`、`APP-INTRO-001`、`APP-INTRO-002`、`TRN-003`、`ASYNC-001`、決定済み `OPEN-005`。
- **入力:** P4b、旧版の応用9問の状態・`targetCells`、型データ、現行版の連続キャプチャ。
- **共通方針:** `index.rebuild.html` を単一の開発正本とし、同じ `#w7-board`、回転、入力、Effect Runner、保存正規化を使う。応用専用の盤面DOM・回転・入力経路は複製しない。`OPEN-005` は、履歴0手なら導入を再生し、1手以上なら安定盤面を直接復元し、導入途中を保存しない契約である。

#### P5-1. 応用9問、`targetCells`、route、保存復元、自己検査

- **作業:** 旧版どおりの応用9問の順序、`state`、`par`、`targetCells` を固定IDカタログへ移す。基本クラス完了後の入口から応用1問目へ遷移し、9問の問題route、0手／1手以上の保存復元、問題IDとカタログの静的自己検査を追加する。
- **完了条件:** 共有盤面で応用1〜9問を開ける。0手の復元は次段階の導入対象として識別でき、1手以上の復元は同じcells/historyへ直接戻る。`state`、`par`、`targetCells`、ID、順序が旧版と一致する。
- **検証:** node抽出による自己検査、`git diff --check`、応用1問目の入口と0手／1手の保存正規化を確認する。E2Eは作らない。
- **コミット境界:** `rebuild(p5-1): add application puzzle catalog and persistence`。

#### P5-2. 完成形見本→右上プレビュー→実盤面→水色形ガイド

- **作業:** 決定済み `OPEN-005` に従い、履歴0手だけに中断可能な導入シーケンスを実装する。完成形見本、右上プレビュー、既存の七枚同時到着、実盤面、水色形ガイドを順番に切り替える。
- **完了条件:** 再訪を含む0手では導入を再生し、1手以上の復元では再生しない。途中フレームは保存されず、motion/reduced-motionとも安定状態へ正規化する。

#### P5-3. 紫枠、回転中の前後関係、誤操作巻き戻し

- **作業:** 問題別の対象三枚へ紫枠をtile描画単位として付け、静止・ドラッグ・通常回転・巻き戻しで前後関係を一箇所から維持する。応用独自の誤操作巻き戻しを共有入力経路に接続する。
- **完了条件:** 対象枠が隣の回転パネルの下へ潜らず、誤手はcells/historyを変えずに戻る。

#### P5-4. クリア後の型別「回し方」、PC/スマホ比較

- **作業:** クリア後に型別の短い「回し方」を表示し、応用先頭・中間・末尾をPCと360pxで比較する。
- **完了条件:** クリア内コツ、導入、誤操作、正解、0手／1手リロードを連続確認し、チェックリストC1・C2の該当項目を完了する。
- **停止:** 見本と実盤面が同時に正本になる、紫枠の所有がoverlayとtileに二重化する、または導入途中を保存する場合。

### P5b. 発展クラス（一括実装、内部4手順）

- **仕様:** `DEV-001`、`TRN-002`、`UI-001` の学園部分、`FLOW-002`・`FLOW-003` の発展完了分。
- **入力:** P5、旧版の発展8問、発展開始／4くるり開始ダイアログ、残り2くるり形データ、誤手除外、発展完了後の九番勝負ゲートの観測結果。
- **共通方針:** `index.rebuild.html` の同じ `#w7-board`、盤面ルール、pointer入力、回転、Effect Runner、保存正規化、共通クリアを使う。発展専用の盤面やドラッグ経路は作らない。発展固有処理は自然な `development` 名で問題データと学園補助の近くへ置き、共通入力側には判定関数の呼び出しだけを残す。P5bでは九番勝負の解放・要求状態までを確定し、時計、問題順、一時停止、試験完走はP7まで実装しない。
- **実行単位:** P5b-1〜P5b-4は別タスクや別コミットではなく、一回の実装依頼の中で守る順序である。8問追加と既存の学園補助の拡張が中心なので、Terra highで一括実装し、全完了条件を満たした一つのコミットで止める。途中で保存正規化・共通入力・形ガイドの共有が崩れる場合だけ、P5b-2完了後を境界に二分する。

#### P5b-1. 8問カタログ、route、保存復元

- **3くるり5問:** 次の値を固定IDの問題データとして移す。最初の正解一手と、その一手後の残り2くるり状態・形名をソルバーから実行時に推測せず、データ近傍へ明示する。

| 問題 | `state` | `par` | 初期候補本数 | 最初の正解 `triangle/direction` | 一手後 `state` | 形 |
| --- | ---: | ---: | ---: | --- | ---: | --- |
| 発展1 | 39 | 3 | 1 | `0 / -1` | 63 | 中と外 |
| 発展2 | 21 | 3 | 2 | `0 / +1` | 76 | ひし形 |
| 発展3 | 93 | 3 | 3 | `3 / -1` | 156 | いも虫 |
| 発展4 | 420 | 3 | 4 | `4 / +1` | 366 | リボン |
| 発展5 | 91 | 3 | 5 | `0 / +1` | 124 | 台形 |

- **4くるり3問:** `state/par/初期候補本数` を順に `364/4/1`、`1121/4/3`、`1391/4/6` として固定する。4くるり側も、初期候補へ必ず含める正解棒を決定的な値としてカタログへ持たせ、再読込のたびに候補がランダムに変わらないようにする。
- 応用9問目の次を「発展開始待ち」、発展1〜8問を `academy-development` routeとして追加する。発展5問目の次は、発展6問目の盤面をまだ操作可能にせず「次は4くるり」descriptorを復元できる状態にする。
- 既知問題ID、`completedPuzzleIds`、navigation、board/historyの正規化対象へ8問を加える。0手、1手以上、クリア確定、開始ダイアログ中、4くるり開始ダイアログ中を、同じ保存値から同じ安定状態へ戻す。
- **完了条件:** 発展1〜8を直接開け、各 `SOLVER.distance[state] === par`、ID・順序・初期候補数・3くるり側の指定一手と一手後状態が一致する。壊れたindex、ID、履歴、盤面は既知の発展安定状態へフォールバックする。
- **検証:** 自己検査で8問の値、ID重複、到達可能性、履歴再生、発展1→5→4くるり案内→6→8のrouteを確認する。

#### P5b-2. 発展開始、学園HUD、候補棒、誤操作

- 応用9問目のクリア後は通常の次問題ダイアログを挟まず、「発展クラス 開始！」を復元可能なdescriptorとして表示する。旧版と同じく発展1の盤面で「あと3くるり→あと2くるり」を見せ、指の点、回る三枚、残り数字の切替、短い行き過ぎから120度へ収まる動きを入力不能な見本SVGで再生する。見本のタイマーはEffect Runnerへ所属させ、閉じた後の処理を残さない。
- 発展5問目のクリア後は「次は4くるり！」を表示し、開始操作で発展6問目へ一度だけ進む。両ダイアログとも再読込後に本文、盤面、ボタン、遷移先をdescriptorから再生成する。
- 発展1〜8は学園HUDを使い、クラス名、問題番号、上部の「あと○くるり」を表示する。現在手数、一手戻す、やり直す、ヒント、応用の右上見本・形の全面発光は出さない。
- 初期盤面だけ問題データの候補本数へ絞る。発展1〜5は指定された最初の正解棒を必ず含め、発展6〜8もカタログで決めた正解棒を含める。一手正しく確定した後は6本すべてを候補へ戻す。候補集合と落下棒はDOMに所有させず、`puzzleId + cells + history` とその挑戦中の一時状態から再構築する。
- 一度でも60度以上回した操作だけを正誤判定する。残り最短手数を1減らす一手は確定し、誤った棒は「そこじゃないよ」で自動巻き戻し後に落下して同じ手では再選択不可、正しい棒の逆方向は「そっちじゃないよ」で巻き戻して正しい棒だけを残す。`+240° = -120°`、`-240° = +120°`、`360° = 0` と素早いフリックは既存の共通入力契約をそのまま使う。
- **完了条件:** 発展1〜8で候補数、正解、誤棒、誤方向、60度未満、240度、往復、キャンセルが定義どおりで、誤操作はcells/history/残り手数を変更しない。正解一手は履歴を1件だけ増やし距離を1減らす。
- **検証:** 発展1・2・5・6・8について、初期候補数、正解棒を含むこと、初手後6本、誤棒落下、誤方向、240度、リロード後の候補再構築を確認する。

#### P5b-3. 残り2くるり形ガイド

- 発展1〜5で、指定された最初の正解一手が確定して残り3から2へ変わった時、盤面中央へ「あと2くるり」と形名を表示し、寝ているだるまの連結外周を水色の太い枠で同時に囲む。通常の数字だけの「あと2くるり」は同時に出さない。
- P5の形外周処理を、応用専用名のまま複製せず、二つ目の実利用として「残り2くるり形ガイド」の自然な共通名へ変更して使う。形の正規化、外周path、見出し、表示／消去は一か所にし、応用・発展それぞれの呼び出し条件だけを近くに残す。
- ガイドは現在の回転確定後に表示し、pointer入力を遮らない。形名、水色枠、背景、文字は同じEffect Runner世代で現れて同時に消え、問題変更、やり直し、非表示化、別ダイアログ、リロードで旧世代を残さない。
- 通常の寝起き切替では、起きただるまへ個別の発光を追加しない。全員起床時の共通クリア演出だけは従来どおり残す。
- **完了条件:** 発展1〜5の一手後に表の形名・外周が一致し、数字だけの案内と重複しない。発展6〜8、誤手の巻き戻し中、初期表示、クリア後には形ガイドを誤表示しない。
- **検証:** 5状態 `63 / 76 / 156 / 366 / 124` を静的検査し、発展1・3・5をPCと360pxで目視する。表示中の次操作、問題変更、リロード、非表示復帰でも古いガイドが残らないことを確認する。

#### P5b-4. クリア、九番勝負解放、フェーズ完了確認

- 発展1〜4・6〜7は共通クリア演出→復元可能なクリアダイアログ→次問題、発展5は共通クリア演出→4くるり開始descriptor、発展8は共通クリア演出→九番勝負要求descriptorへ、一度だけ遷移する。
- 発展8のクリア確定時点で8問の完了記録から「九番勝負を選べる」ことを導出し、発展完了後の安定状態として保存する。解放をボタンの表示状態だけに持たせない。P7はこの状態へ九番の開始操作とセッションを接続するため、P5bでは動かない時計、仮の問題、壊れた開始ボタンを先に置かない。
- 開発用ボタンには、発展開始、発展5問目直後、発展全問クリア直後へ移る入口を追加する。いずれも本番と同じAppState factoryとdescriptorを使い、テスト専用の不可能な状態を作らない。
- **完了条件:** 応用9→発展開始→8問→九番要求まで通り、発展8クリア直後の連続操作・リロードでも二重記録や通常の次問題への遷移がない。九番未完了のまま修行へ進めず、P7が読む解放条件を保存DOMではなく状態から判定できる。
- **検証:** 発展1の0手／1手、発展5クリア、4くるり開始ダイアログ、発展8クリア演出中／要求ダイアログ中を再読込する。PCと360pxで開始見本、HUD、候補棒、形ガイド、両境界ダイアログを比較する。E2Eは作らず、構文、自己検査、コンソール例外なしの最小起動確認までとする。

- **自然検索レビュー:** 実装者とは別の文脈で「発展クラスで間違えた棒を落とす」「3くるりから2くるりになった時の形ガイド」「発展クリア後に九番勝負を出す」とだけ依頼し、検索語を指定せず `rg` させる。到達した周辺から問題データ、候補、正誤判定、表示、保存、境界を説明できることをP5bの受入れ条件にする。
- **コミット境界:** `rebuild(p5b): add development class and speed-nine entry`。
- **停止:** 発展専用の盤面・pointer処理を複製する、候補や落下棒をDOMだけが所有する、形ガイドを問題番号だけで出す、形ガイドと通常残り表示が重複する、九番解放をボタンの有無だけで表す、または九番の未実装セッションへ進める場合。

### P6. 修行、名人、悟り、節目分岐

P6は一度に実装せず、`P6a 修行`、`P6b 名人`、`P6c 悟り` の順に、各段階を独立した実装・レビュー・コミットにする。各段階で既存のBoard Engine、pointer処理、Effect Runner、Modal Manager、保存コマンドを再利用し、モード別の盤面や入力経路を複製しない。

九番・十八番・二十七番の速解き本体はP7で実装する。P6では各試験の完走後に受け取る安定したroute descriptorとAppState factoryを定義するが、未実装の時計・仮問題・試験を通過したふりをする公開ボタンは作らない。P6単独の確認は、本番と同じ状態factoryを使う開発用入口から行い、P7で試験完走コマンドをその入口へ接続する。

#### P6a. だるま修行 上巻・中巻・下巻

- **仕様:** `TRN-001`〜`TRN-004`、`FLOW-002`、`FLOW-003`。
- **実装対象:** `index.rebuild.html` 一枚だけ。現行版 `index.html` は観測とデータ照合にだけ使い、変更しない。
- **P6aに含めるもの:** 修行27問、三巻の通常プレイ、周回別進行、九番完走後の入口契約、三つの開始chain、上巻のコツ、全巻のあと2くるり形ガイド、巻境界、十八番要求、開発用入口、保存復元、自己検査。
- **P6aに含めないもの:** 九番・十八番の時計と出題セッション、メニュー／問題選択画面、名人、悟り、設定画面、文言の多言語化。これらの空DOMや仮ボタンも置かない。

##### P6aで固定する問題・形データ

上巻は基本9型を別の向きで解く。同じ型の名称、回し方、形判定、応用クリア見本を別々の表にしない。既存の `APPLICATION_SHAPE_NAME_BY_SOURCE` と `APPLICATION_CLEAR_TURN_HINT_BY_SHAPE` は、次の共有2手形カタログへ統合し、応用と修行が同じエントリを参照する。応用の表示と動作はこの整理で変えない。

| 位置 | 基準状態 | 上巻状態 | 形ID | 表示名 | 原理 |
| ---: | ---: | ---: | --- | --- | --- |
| 1 | 922 | 1812 | `trapezoid` | 台形 | `join-one` |
| 2 | 1516 | 882 | `caterpillar` | いも虫 | `join-one` |
| 3 | 526 | 1086 | `trapezoid` | 台形 | `join-one` |
| 4 | 544 | 468 | `caterpillar` | いも虫 | `join-one` |
| 5 | 1732 | 1004 | `ribbon` | りぼん | `join-one` |
| 6 | 196 | 44 | `diamond` | ひし形 | `join-two` |
| 7 | 76 | 550 | `diamond` | ひし形 | `join-two` |
| 8 | 7 | 165 | `outer-neighbor` | 外隣り | `join-two` |
| 9 | 57 | 783 | `center-outer` | 中と外 | `join-two` |

- 上巻9問は固定ID `training-upper-001`〜`009`、上表の上巻状態、全問最短2手とする。各問題は `patternPosition` を持ち、形名を問題番号の条件分岐から求めない。
- 中巻9問は固定ID `training-middle-001`〜`009`、状態 `[21, 15, 99, 13, 39, 93, 91, 117, 380]`、全問最短3手とする。
- 下巻9問は固定ID `training-lower-001`〜`009`、状態 `[104, 358, 426, 582, 332, 344, 325, 146, 200]`、全問最短4手とする。
- 上巻開始の形一覧は基準状態 `[922, 1516, 1732, 196, 7, 57]` と形 `[台形, いも虫, りぼん, ひし形, 外隣り, 中と外]`、中巻は `[21, 99, 13, 39, 93, 117, 380]` と `[猫の耳, 両岸, お皿, ネックレス, 直角三角形, 一直線, 手裏剣]`、下巻は `[104, 358, 426, 332, 325, 146]` と `[王冠, 弓矢, 長方形, 気球, 大三角, いも虫]` を固定データにする。
- 2手形の判定は九つの基準状態を `VIEW_SYMMETRIES` で正規化したキーから行う。距離2でも九型に属さない盤面は形ガイド対象外とし、表示側で最も近い形を推測しない。
- 起動時自己検査で27問のID・順序・重複・state範囲・solver距離、上巻と基準9型の対称同値、形一覧のstateと名称対応を照合する。

##### P6a-1. 保存状態を周回対応へ拡張する

P6aの最初に進行状態を次へ更新し、以後のUI実装はこの形だけを使う。

```text
progress
├─ tutorialCompleted
├─ tutorialCompletedPuzzleIds
├─ lap1CompletedPuzzleIds
└─ lap2CompletedPuzzleIds

speed
└─ completedTrialIds  // training9 / training18 / mastery27
```

- 現在の曖昧な `completedPuzzleIds` は残さず、チュートリアルと各周の問題を名前で分ける。参照側は `selectCompletedPuzzleIdsForLap(progress, lap)` を通し、画面ごとに三項演算子を散らさない。
- 試験完走は進行問題とは別に `AppState.speed.completedTrialIds` が所有する。P6aで実際に読む試験IDは `training9` だけだが、P6b・P6c・P7が同じ配列へ `training18` / `mastery27` を追加できる形にする。P6aから未実装試験を完了扱いにしない。
- 状態形式変更のため `APP_SCHEMA_VERSION` を2から3、問題追加のため `APP_CATALOG_VERSION` を4から5へ上げる。開発中のV2保存は一度初期化されてよく、旧形式を延命する移行コードは作らない。旧版 `index.html` の保存領域には触れない。
- `normalizeCourseProgress`、完了記録、開発用進行生成、既存の学園完了判定を周回selector経由へ変える。P4・P5のチュートリアル／学園の開始、途中復元、デバッグボタンが変わらないことをこの段階で確認する。
- `navigation.lap` は1または2だけを許可し、問題完了は必ず現在lapの集合へ記録する。二周目の公開入口はまだ作らず、正規化とfactoryが二周目を扱えるところまでにする。

##### P6a-2. 修行カタログ、route、ゲートを接続する

- `routeId` は `training-upper` / `training-middle` / `training-lower`、`courseId` は `training`、`sectionId` は `upper` / `middle` / `lower`、`mode` は既存どおり `stage` とする。各巻の `puzzleIndex` は0〜8、保存の照合は固定 `puzzleId` で行う。
- 三巻のcatalog selector、navigation factory、AppState factory、同じ巻の最初の未クリアへ戻るrecoveryを、既存の学園route関数に隣接して追加する。壊れた中巻navigationを上巻や入門へ戻さず、現在lapの中巻で最初の未クリア、全問済みなら中巻最終問題の節目状態へ正規化する。
- `selectTrainingEntryGate(appState, lap)` は、学園発展8問完了を前提に、一周目なら `AppState.speed.completedTrialIds` に `training9` がある場合だけ許可し、二周目なら九番フラグなしで許可する。結果は真偽だけでなく `development-incomplete` / `speed-nine-required` / `allowed` を返し、表示文言を判定値にしない。
- P7が九番完走後に呼ぶ入口は一つにし、許可時だけ `training-welcome` descriptorを持つ上巻AppStateを返す。P6aでは `speed-nine-required` の公開ボタンからこの入口を呼ばない。開発用入口だけが、九番完走を含む有効な前提状態をfactoryで作る。
- `selectPuzzleForNavigation`、保存navigationの許可リスト、盤面履歴の検証、`stableDialogForNavigation` に三routeを追加する。修行の0手・1手・クリア済み盤面をすべて同じ保存検査へ通し、修行専用の保存形式を作らない。

##### P6a-3. 通常プレイと修行用HUDを完成させる

- 上中下巻とも既存の `#w7-board`、六本の棒、pointer入力、連続回転、Undo、Restart、到着演出、クリア演出をそのまま使う。学園の候補棒、60度正誤判定、誤手巻き戻し、応用の対象強調、発展の落下棒をpolicyで無効にする。
- HUDは `だるま修行・上巻／中巻／下巻`、`1 / 9`、`いま N くるり`、`あと N くるり` を現在navigationと盤面から描画する。上巻だけ「コツを見る」をUndo／Restartと同じ操作領域へ追加し、中巻・下巻ではDOMを削除せずrendererが非表示にする。
- 共通描画へ修行固有の分岐を連ねず、修行の画面モデルを返すselectorを一か所に置く。既存の `renderBoardStatus`、`renderRuntime`、操作ボタン描画は、その戻り値を使うだけにする。
- 通常問題の「次へ」は、同じ巻の1〜8問目では次の固定IDへ進み、盤面・履歴・一時効果を初期化して到着演出を一回だけ開始する。既存の `intentNextAcademyPuzzle` を巨大化させず、同一契約だけを共通の「次問題」コマンドへ出し、巻境界は後述の修行進行に残す。

##### P6a-4. あと2くるり形ガイドと上巻のコツを実装する

- 回転プレビュー中ではなく、一手がAppStateへ確定した直後だけ `selectTrainingTwoMoveShape(cells)` を呼ぶ。実距離が2で九型の正規形に一致すれば、中央に「あと2くるり」＋形名、寝ているセル外周に水色の太枠を一つのEffect Runner効果として出す。
- 表示の発火条件に「前が3手だったか」を入れない。同じ距離2から別の距離2へ確定移動した場合も新しい効果を開始する。Undo、Restart、復元、ダイアログ見本の描画では発火させない。
- ガイド開始時に通常の中央残り表示を閉じ、ガイド終了後は現在のAppStateから通常HUDを再描画する。旧タイマーが次問題へ作用しないよう、問題変更・モーダル開始・Undo・Restart・pagehideで世代を無効化する。
- 水色外周枠は既存の応用／発展形ガイドの「寝たセルをかたどる」純粋なpath生成を共用する。修行用は色や時間を変えるだけで、同じ形状計算を複製しない。
- 上巻の「コツを見る」は現在問題の `patternPosition` から共有2手形データを引き、`「○○」のコツ`、見本盤面、回転三枚と水色の点のアニメーション、「選び方のコツ」「回し方のコツ」、前後の型移動を表示する。現行版の `openTwoMoveLessonDialog` と `buildTwoMoveLessonBoard` を観測元にする。
- コツの見本は現在問題をそのまま答えとして静止表示せず、現行版同様に表示用対称変換を施す。アニメーションで使う一手はsolver列挙順へ暗黙依存させず、共有2手形データに固定した `demonstrationMove` を持たせ、自己検査で距離が一つ減ることを確認する。
- 応用クリア後の型別回し方は共有2手形カタログと共通見本rendererを引き続き使う。P6a完了時に応用1・5・9の見本、発光周期、回転対象が変わっていないことを手動確認する。

##### P6a-5. 三つの開始chainと巻境界を実装する

保存するdescriptorと順序を次で固定する。各ボタン操作のたびに次descriptorをAppStateへ保存してから描画し、再読込時は同じ段階を再描画する。アニメーションの途中時刻は保存せず、ダイアログを最初のフレームから再開する。

```text
training9完走後
  training-welcome
  → training-upper-goal
  → training-upper-practice
  → training-upper-001

training-upper-009クリア
  training-middle-goal
  → training-middle-spin
  → training-middle-001

training-middle-009クリア
  training-lower-goal
  → training-lower-001

training-lower-009クリア
  speed-eighteen-required
```

- 歓迎、形一覧、上巻前半実演、中巻連続回転、下巻形一覧は、共通 `renderDialog` の巨大な条件分岐へ中身を散らさない。修行descriptorを受けて内容を構築する `renderTrainingDialog` と、その操作intentを修行機能の連続した範囲へ置き、共通rendererには呼び出しだけを追加する。
- 上巻の文言は「だるま修行へようこそ！」→「上巻ではこんな形が登場するよ」→「『あと2くるり』の修行だよ！」→「上巻前半では、外側の寝た2枚に、1枚を合わせにいこう」を現行版の表示・改行と比較する。
- 中巻形一覧の次に、一本の下側の棒を途切れず回し、距離3→2→3→2、回転する三枚、タッチ点、カウンターを見せる。開始時は六本の棒を表示し、タッチ点が出る時に棒を隠す。現行版の `buildTrainingMiddleSpinBoard` を観測元とし、P5bの発展開始見本を変更しない。
- 下巻は形一覧の後に下巻1へ進む。現行版に存在しない余分な練習画面を追加しない。
- 上巻9・中巻9・下巻9のクリア確定コマンドは、問題完了IDと次descriptorを同じ同期更新で一度だけ保存する。クリア演出終了は保存済みdescriptorの描画だけを要求し、次routeを再計算・再保存しない。
- 下巻9では `speed-eighteen-required` を保存し、P7未実装の十八番へは進めない。ボタンはP7まで安全な閉じ方または再挑戦だけとし、動かない開始ボタンを置かない。

##### P6a-6. 復元、開発用入口、自己検査を仕上げる

- 開発用ボタンは「修上」「修中」「修下」「修完」を追加する。「修上」は一周目学園完了＋`training9`完走済み＋`training-welcome`、「修中」「修下」は前巻まで完了済み＋各開始descriptor、「修完」は27問完了済み＋下巻9の解決盤面＋`speed-eighteen-required` を、本番と同じfactoryから作る。
- 既存の「即」は修行でも本物のクリア確定と同じ境界を通す。上巻／中巻の1〜8問目はクリアダイアログ、各巻9問目は次の開始descriptor、下巻9問目は十八番要求になること。デバッグ専用の遷移表を別に持たない。
- 復元は、通常プレイ0手／1手、クリアダイアログ、各開始descriptor、形一覧、上巻実演、中巻実演、十八番要求を許可リストで再構築する。形ガイド表示中の再読込はガイドを捨て、同じ問題の確定盤面へ戻す。
- 自己検査名は機能を表す `training-catalog`、`training-shape-recognition`、`training-entry-gate`、`training-navigation-recovery`、`training-boundaries`、`training-dialog-restore` とする。検索用タグではなく、失敗内容をその場で説明できる実検査にする。
- `stageReferenceCount` は修行27問を含む値へ更新する。自己検査は、九型の全対称で同じ型を認識すること、非2手状態で形を返さないこと、一周目九番未完了を拒否／完了を許可すること、二周目は九番不要でも同周の発展完了を要求すること、三つの巻末が正しいdescriptorへなることを検証する。

##### P6aの実行順と確認点

1. P6a-1だけ実装し、初回・途中チュートリアル・基本・応用・発展の保存復元と既存デバッグ入口を確認する。ここで既存進行が壊れたら先へ進まない。
2. P6a-2〜3を実装し、開発用の上巻入口から27問を六本の棒で操作でき、Undo／Restart／残り手数／到着・クリア演出が共通実装で動くことを確認する。
3. P6a-4を実装し、上巻1・5・9、中巻1・9、下巻1・9で、距離2への到達、距離2間の移動、距離2から離脱、Undo、Restartを確認する。上巻1・6・9のコツと応用1・5・9の既存見本も比較する。
4. P6a-5〜6を実装し、全開始chainと巻末を通常操作・「即」・再読込の三経路で確認する。
5. PCと360pxで、上巻開始三画面、中巻形一覧と連続回転、下巻形一覧、通常HUD、上巻のコツ、形ガイド、十八番要求を現行版と並べて確認する。
6. 構文、`git diff --check`、起動時自己検査、コンソール例外なしを確認する。E2Eは作らない。
7. Terra highの実装完了後、コミット前にSol highで仕様差、状態遷移、復元、既存P4・P5回帰、自然検索性をレビューし、指摘を直してから一つのP6aコミットにする。

- **完了条件:** 27問の順序と最短距離が固定され、上巻開始から下巻完了・十八番要求まで通る。形ガイド、上巻のコツ、中巻実演、三つの巻境界が連打・戻る・再読込でも重複せず、二周目の試験省略も本来の周回条件から導出される。P4・P5の観測済み挙動に未説明の差分がない。
- **自然検索レビュー:** 実装者が検索語を指示せず、別文脈のAIに修行の問題順、あと2くるり形ガイド、上巻のコツ、中巻連続回転、巻末遷移のいずれかを変更する想定で自然に `rg` させる。日本語・英語のどちらを使ってもよい。修行固有のデータ・判定・表示・効果・保存・次routeは `index.rebuild.html` 内の連続した修行範囲から追え、共通部にあるのは名前付きの小さな接続点だけであること。検索専用の別名コメントや索引に頼らないことを受入れ条件にする。
- **コミット境界:** `rebuild(p6a): add daruma training routes`。計画・実装・レビュー修正を含め、P6bには着手しない。
- **停止:** 修行専用Board Engineを作る、進行状態を周回別へ分けないまま追加する、形判定が問題番号・直前距離・solver列挙順だけに依存する、同じ型の見本データを複数箇所に持つ、修行descriptorの中身が共通renderer各所へ散る、巻末が通常次問題と節目routeの両方へ進み得る、またはP7未実装の九番／十八番セッションへ進める場合。

#### P6b. 名人への道 45問

- **仕様:** `MST-001`、`FLOW-002`、`FLOW-003`。金色盤面の獲得はP6bではなく、P7の二十七番勝負初回完走が所有する。
- **実装対象:** `index.rebuild.html` 一枚だけ。現行版 `index.html` は問題順、表示、最短失敗、残り手数確認、節目ダイアログの観測元にだけ使い、変更しない。
- **P6bに含めるもの:** 名人への道45問、序・破・急のrouteとHUD、十八番完走後の入口契約、最短手数必須判定、失敗と再挑戦、序の一手ヒント、急の残り手数確認、二つの巻境界、二十七番要求、周回別進行、開発用入口、保存復元、自己検査。
- **P6bに含めないもの:** 十八番・二十七番の時計と出題セッション、問題選択画面、悟り73問、金色盤面の付与・表示・設定、クリア雑学の全移植、多言語化。未実装機能の空DOMや開始できる仮ボタンも置かない。

##### P6bで固定する問題・巻データ

三巻を別々の条件分岐へ散らさず、名人機能の先頭に一つの巻カタログとして置く。各巻エントリは `routeId`、`sectionId`、表示名、副題、問題一覧を同じ場所に持つ。問題IDは巻内番号が読み取れる固定IDとし、solver列挙順から実行時生成しない。

| 巻 | routeId | 表示 | 問題ID | state | par |
|---|---|---|---|---|---:|
| 序 | `mastery-jo` | `名人への道・序` / `七転八起` | `mastery-jo-001`〜`015` | `[5,150,377,176,202,382,457,390,178,420,148,52,106,122,619]` | 3 |
| 破 | `mastery-ha` | `名人への道・破` / `面壁九年` | `mastery-ha-001`〜`015` | `[395,372,1105,611,98,617,348,356,442,132,410,449,362,338,580]` | 3 |
| 急 | `mastery-kyu` | `名人への道・急` / `不立文字` | `mastery-kyu-001`〜`015` | `[1159,403,1157,1097,455,401,1066,1079,1183,346,418,340,1391,1121,364]` | 4 |

- 45問をまとめて参照する場合は上の巻カタログを平坦化した一つの派生値を使い、同じstate配列やID表を別途持たない。
- 起動時自己検査で、各巻15問、合計45問、固定順、固定ID、state重複なし、序・破は距離3、急は距離4を照合する。
- `selectPuzzleForNavigation`、保存navigationの許可リスト、履歴検査、進捗IDの許可リストへ三routeを接続する。名人専用の盤面形式やBoard Engineは作らない。

##### P6b-1. 入口、route、通常プレイを接続する

- `mode:'stage'`、`courseId:'mastery'`、`routeId:'mastery-jo' / 'mastery-ha' / 'mastery-kyu'`、`sectionId:'jo' / 'ha' / 'kyu'`、`lap:1 / 2` とする。各巻の `puzzleIndex` は0〜14で、復元時は固定 `puzzleId` とindexの両方を照合する。
- `hasCompletedTrainingCourse(progress, lap)` は同じ周回の修行27固定IDだけを見る。`selectMasteryEntryGate(appState, lap)` は `training-incomplete` / `speed-eighteen-required` / `allowed` を返し、一周目だけ `speed.completedTrialIds` の `training18` を要求する。二周目は試験を省略するが、同じ周回の修行完了は省略しない。
- P7が十八番完走後に呼ぶ入口は `makeMasteryEntryAppState` 一つにする。許可時だけ、序1問目と `mastery-welcome` descriptorを持つ安定AppStateを返す。P6bの公開UIから未実装の十八番を完走したことにはしない。
- 開始案内は現行版の「だるま修行 修了！」「だるま修行を終え、名人への道が開かれました。」「名人への道・序　七転八起へ →」を基準にする。案内を閉じた時だけ序1問目の到着演出へ進む。
- HUDは巻名、副題、巻内 `1 / 15`、現在手数、残り最短手数を名人用screen modelから描く。共通 `renderBoardStatus` や操作欄には、screen modelを読む小さな接続点だけを加える。
- 盤面、六本の棒、ドラッグ、連続回転、起き寝、Undo、Restart、到着・クリア演出はP6aまでの共通実装を使う。名人専用のSVGやpointerイベントを作らない。
- 序だけ「次の1手を見る」を使えるようにする。solverから距離を一つ減らす一手を見せるだけで盤面・履歴・進行を変更しない。破では同じ操作位置を「ヒントが使えなくなります」という旧版の規則に合わせて使用不可にし、急では同じ位置を後述の「残り手数」へ切り替える。

##### P6b-2. 最短手数必須と失敗・再挑戦を一つにする

- 盤面が解決した直後、進捗や `clearCommitted` を更新する前に、名前付きの純粋判定（例: `selectOptimalClearOutcome`）へ `solved`、確定手数、問題par、route policyを渡す。結果は `not-solved` / `optimal-clear` / `over-limit` とし、P6cも同じ判定を使える位置へ置く。
- `optimal-clear` の時だけ問題IDを周回別完了集合へ追加し、共通クリア演出へ進む。`over-limit` は完了集合へ追加せず、通常クリア演出も通常クリアダイアログも出さず、`optimal-fail` descriptorを保存して失敗ダイアログを開く。
- `optimal-fail` は最低限 `puzzleId`、`bestMoves`、`actualMoves` を持つ。表示は差が1手なら「おしい！あと1手短縮できればクリア。」、2手なら「もう少し！あと2手短縮できればクリア。」、それ以上なら「回し方を変えて最短への道を探そう！」、続けて最短手数・今回手数・「再挑戦する」を出す。
- 解決済み盤面＋`optimal-fail` の保存を通常クリアへ誤変換しない。復元は失敗descriptorを先に検証し、同じ問題の失敗ダイアログとして戻す。descriptorの問題IDや手数が不正なら、その問題の未クリア初期盤面へ安全に戻す。
- 再挑戦は同じ問題・同じ周回の初期盤面へ戻し、履歴、`clearCommitted`、失敗descriptor、一時演出を消す。後述の残り手数確認回数は「最短超過後の再挑戦」だけ規則どおり回復させる。
- この判定、descriptor正規化、再挑戦intentを名人機能の近接範囲へまとめ、共通クリア側には「解決後の結果を問い合わせる」一つの接続だけを残す。

##### P6b-3. 急の残り手数確認を保存する

- 急31〜45問は通常の「あとNくるり」を常に `?` とする。同じ操作欄のボタンを `残り手数 ×残り回数` とし、押した時だけ現在のsolver距離を表示する。31〜36問は2回、37〜42問は1回、43〜45問は0回とする。
- 使用回数はDOM、巻内index、履歴へ持たせず、AppStateの名人領域へ `lap1 / lap2 → puzzleId → usedCount` の形で保存する。schema更新時は既知の急問題IDだけを許可し、整数へ正規化して問題ごとの上限にclampする。一周目の使用回数を二周目へ持ち込まない。
- 「いま開示中か」はRuntimeStateだけが持つ。クリック直後は現在距離を表示し、次の回転開始、Undo、Restart、問題移動、ダイアログ表示、hidden/pagehide、再読込で `?` に戻す。再読込で使用済み回数は残すが、開示中表示は復元しない。
- Undoでは使用回数を戻さない。Restartは名人31〜39問では当該問題の使用回数を0へ戻し、40〜42問では維持する。43〜45問は常に0回で変化させない。最短超過後の「再挑戦する」は31〜42問すべて0へ戻す。
- 上限、残数、使用、Undo、Restart、失敗再挑戦の規則は、自然に `mastery remaining` / `残り手数` で見つかる一つの連続したまとまりに置く。イベントハンドラごとに回数更新を散らさない。

##### P6b-4. 通常次問題、巻境界、二十七番要求を確定する

- 序1〜14、破1〜14、急1〜14は、最短クリア後に共通クリアダイアログを出し、「次へ」で同じ巻の次の固定IDへ進む。盤面・履歴・開示中表示・一時効果を初期化して到着演出を一度だけ始める。
- 序15問目は通常クリアダイアログを省き、完了IDの記録と同時に破1問目＋`mastery-ha-welcome` を安定状態として保存する。ダイアログは「名人への道・序　七転八起 クリア！」、称号「不屈」、次巻ではヒントが使えないことを示す。
- 破15問目も同様に、急1問目＋`mastery-kyu-welcome` を一度だけ保存する。ダイアログは「名人への道・破　面壁九年 クリア！」、称号「熟練」、急は最短4手で通常の残り手数が `?`、回数限定の確認を使うことを示す。
- 急15問目は最短クリアの完了IDと名人45問完了を確定し、解決済み最終盤面＋`speed-twenty-seven-required` を保存する。P6bでは二十七番を開始せず、「名人」称号、金色報酬、現在テーマを付与しない。
- 巻末の行先は本番クリア、デバッグ即クリア、保存復元が同じ `makeMasteryBoundaryDestination` を使う。クリア演出callbackが進捗や行先を再計算しない。演出中にhidden/pagehideとなっても、確定済みの次巻または二十七番要求へ一度だけ戻る。

##### P6b-5. 開発用入口、復元、自己検査を仕上げる

- 下部の開発用操作へ、名人序開始、破開始、急開始、14問完了、29問完了、44問完了、45問完了直後を追加する。短い表示名でも `title` とintent名は自然な `mastery` / `名人` 検索で意味が分かるようにする。
- 開発用入口は、同じ周回の修行27問完了を正規factoryで作る。一周目は `training18` 完走済みも含め、二周目入口は二周目修行完了を含める。問題だけを直接差し替えたり、画面要素でゲートを迂回したりしない。
- 既存の「即」は名人でも本番の最短クリア確定と同じ経路を通る。最短超過の確認用には、盤面を解決させて履歴をpar+1に偽装せず、履歴検査に通る実手順を作れる場合だけ専用デバッグ状態を用意する。
- 復元では、0手、途中履歴、最短失敗、通常クリア、序15・破15・急15の確定直後、残り手数使用済みを検査する。壊れたrouteは同じ巻の最初の未完了固定IDへ戻し、別コースへ飛ばさない。
- 自己検査は `mastery-catalog`、`mastery-entry-gate`、`mastery-optimal-clear`、`mastery-remaining-check-policy`、`mastery-navigation-recovery`、`mastery-boundaries`、`mastery-dialog-restore`、`mastery-reward-boundary` とする。検索用タグではなく、各失敗理由をその場で説明する実検査にする。
- `stageReferenceCount` は名人45問を含む値へ更新する。`mastery-reward-boundary` は45問完了後も `rewards.masterGoldGranted === false`、`settings.theme` が勝手に `gold` にならず、`speed.completedTrialIds` に `mastery27` が追加されないことを確認する。

##### P6bの実行順と確認点

1. **接続:** カタログ、三route、同周回ゲート、screen model、通常盤面を接続する。序・破・急の各1問目を六本の棒で操作でき、既存の修行・応用・発展に表示回帰がないところで一度確認する。
2. **最短フロー:** 最短判定、`optimal-fail`、再挑戦、通常次問題、序15／破15／急15境界を接続する。最短成功と1手超過を、保存・再読込を挟んで確認する。
3. **急の制限:** 残り手数確認、周回別使用回数、Undo／Restart／失敗再挑戦、開発用入口、自己検査を仕上げる。上の実行順1〜3は内部の作業順であり、最終的には一つのP6bコミットにする。
4. 各段階で構文、`git diff --check`、起動時自己検査、コンソール例外なしを確認する。E2Eは作らない。画面の最終判断はユーザーの手動確認を優先する。
5. PCと360px相当で、序1・15、破1・15、急1・6・7・9・10・12・13・15、最短失敗を確認する。盤面位置、棒、回転、クリア演出はP6aまでの共通表示から変えない。
6. Terra highで実装後、コミット前にSol highで仕様差、最短失敗の保存、確認回数、周回分離、巻境界、P4〜P6a回帰、自然検索性をレビューする。指摘対応と再レビューが終わるまでP6cへ進まない。

- **完了条件:** 45問が固定順で遊べ、最短手数ちょうどだけが進行を記録する。三つの巻境界と二十七番要求が一度だけ確定し、急の確認回数がUndo・Restart・失敗・再読込の契約どおりに残る。名人45問完了だけでは金色盤面も「名人」称号も得ない。
- **自然検索レビュー:** 別文脈のAIに、検索語を指定せず「名人で最短手数を超えた時」「終盤の残り手数を見る回数」「名人45問後の二十七番待ち」のいずれかを調べさせる。自然な日本語・英語の `rg` から、問題カタログ、最短判定、確認回数、Undo／Restart、保存、巻末routeまで追え、その周辺から目的と壊してはいけない規則を説明できること。検索専用タグだけ当たる構造、共通関数を巡回しないと名人固有ルールを復元できない構造は不合格とする。
- **コミット境界:** `rebuild(p6b): add path-to-mastery routes`。P6aを先に別コミットへ確定し、P6bの計画・実装・レビュー修正を一つにまとめる。pushは依頼があるまで行わない。
- **停止:** P6aが未コミットのままP6b実装を混ぜる、名人専用Board Engine／pointer処理を作る、最短超過を一度クリア保存してから取り消す、確認回数をDOM・配列位置・共通履歴だけで所有する、一周目と二周目で確認回数が混ざる、Undo／Restartの規則が一か所から読めない、45問完了で金色報酬や名人称号を付与する、またはP7未実装の二十七番を開始できる場合。

#### P6c. 悟り 73問と悟り盤面報酬

- **仕様:** `MST-001`、`FLOW-002`、`FLOW-003`、`REWARD-001` の悟り報酬部分。
- **入力データ:** 現行版の悟り73問を表示順どおり、固定問題ID・初期状態・最短手数の明示的なカタログとして移す。内訳は最短1手が1問、2手が9問、3手と4手が63問で、実行時に全73件の距離、重複、固定順を自己検査する。solverの列挙順やオブジェクトの並び順からカタログを再生成しない。
- **到達条件:** 一周目は名人45問完了かつ二十七番勝負完走後の `speed-twenty-seven-cleared` だけが悟り開始案内へ接続できる。二周目は九番・十八番・二十七番の試験フラグを要求しないが、その周の名人45問完了を必須とする。条件不足時は画面要素の有無ではなく状態判定から一意な拒否理由を返す。
- **プレイルール:** P6bと同じ最短手数必須の判定・失敗descriptor・再挑戦を使う。悟りではUndoとヒントを出さず、残り最短手数は `?` とする。二周目は成功するまで最大4手目まで操作でき、4手目でも未解決なら失敗descriptorへ進む。盤面、入力、回転、クリア演出は共通実装のまま、悟り専用差分はpolicyとカタログの近くで読めるようにする。
- **途中進行:** 問題ID、問題位置、周回、盤面、手数、保留中descriptorを保存し、通常問題は共通クリア演出から次の悟り問題へ一度だけ進む。開始案内中、最短失敗中、クリア演出中、73問目確定直後の再読込でも問題を飛ばしたり重ねたりしない。
- **最終問題と解放:** 一周目73問目のクリア確定コマンドが、悟り完了記録、七十三番勝負の解放資格、`rewards.satoriDesignGranted`、初回の悟りテーマと30度傾斜配置の自動適用、最終descriptorを一度に冪等確定する。演出終了コールバックから報酬を保存しない。報酬獲得資格と現在選択を別フィールドにし、ユーザーがまだ設定画面で選んでいないことを表す `themeChosen` / `layoutChosen` と混同しない。
- **悟り盤面表示:** 悟りテーマと30度傾斜配置は獲得直後から描画できるところまでP6cで実装する。傾斜は盤面全体の表示変換だけで、論理セル、棒ID、当たり判定の対応、履歴、solver結果を変えない。だるまは逆向きに30度補正して画面に対して正立させる。設定画面からの再選択UIと全設定の仕上げはP8に残す。
- **二周目:** 一周目悟り完了後の周回解放状態と、二周目悟りの73問進行を一周目と分離して保存する。二周目開始時に一周目の問題クリア集合、報酬獲得、現在テーマを消さない。二周目73問目の再クリアでは報酬付与や初回自動適用を繰り返さない。
- **開発用入口:** 悟り開始、72問クリア済み、最終問題クリア直前、二周目悟り開始、二周目72問クリア済みへ正規factoryから移れる入口を追加する。入口は必要な名人完了状態を同じ周回へ作り、公開ゲートそのものを迂回する特別分岐は作らない。
- **完了条件:** 一周目と二周目の73問が固定順で進み、最短失敗、4手上限、途中復元、最終分岐が契約どおり動く。初回悟り報酬は二重イベント・連続再読込でも一度だけ付与され、30度表示でも同じ棒操作が同じ論理結果になる。未獲得状態から設定値だけを壊して悟りテーマを選べない。
- **検証:** 悟り1手・2手・3手・4手問題で最短成功と超過を確認し、二周目は1〜4手の未解決経路も確認する。72→73、73クリア演出中、報酬確定直後、最終descriptor中で再読込し、報酬・七十三番解放・進行が一度だけ保存されることを確かめる。通常配置と30度配置で同じ棒を同方向へ回して、同じ盤面状態になることも確認する。PCと360pxの最小比較、構文、自己検査、コンソール例外なしの起動確認を行う。E2Eは作らない。
- **自然検索レビュー:** 別文脈のAIに検索語を指定せず、悟りの問題順、二周目4手制限、最終クリア報酬、30度盤面のいずれかを調査させる。AIが自然に選んだ日本語・英語の `rg` から、カタログ、policy、最短判定、保存、最終コマンド、描画変換まで辿れ、その周辺から変更目的と壊してはいけない境界を説明できることを受入れ条件にする。
- **コミット境界:** `rebuild(p6c): add satori route and board reward`。
- **停止:** 悟り専用Board Engineやpointer処理を作る、カタログ順をsolver出力へ依存させる、報酬を演出callbackが付与する、獲得資格と現在選択を同じ値で表す、傾斜が論理座標を変える、または二周目の試験省略が名人完了条件まで外す場合。

### P7. 速解き、自由、自作（5段階）

P7は時計を持つ連続問題、通常コースとの試験合流、独立した自由セッション、盤面編集を同時に変更しない。共通盤面を使う点以外は状態の寿命と失敗条件が異なるため、P7a〜P7eを別コミットにし、前段が安定してから次へ進む。

`OPEN-007` は「通常リロード後も一時停止状態で復元し、明示的な再開まで時計を止める」、`OPEN-008` は「最後の有効な自由セッションを永続化し、別モードでリロードした後も自由への再入場時に復元する」で2026-09-15に決定済みである。

#### P7a. 速解き共通基盤と九番勝負

- **仕様:** `SPD-001` の九番部分、`SPD-002`〜`SPD-004`、`OPEN-007`。
- **入力:** P6cが別コミットとして確定した `index.rebuild.html`、基本2くるり9問、九番の現行UI・候補棒・開始待ち・pause・F5比較。
- **状態と時計:** `awaiting-start`、`running`、`paused`、`advancing`、`finished` の安定状態を一つの速解きセッションが所有する。保存する経過時間は完了済み実行区間の累積値とし、`performance.now()` は現在のJavaScript実行環境でrunningになった区間だけに使う。pause、hidden、pagehide、問題確定の各境界で同じ区間を二重精算しない。復元時に壁時計との差分を加算しない。
- **開始・復元:** 最初は解決済み盤面と開始ボタンを表示し、時計を動かさない。開始操作後に第1問を共通到着演出で出す。通常リロードとvisibility復帰は、runningだったセッションをpausedとして同じ問題・順序・盤面・累積時間へ復元し、明示再開を要求する。開始待ちは開始待ちへ戻す。解決確定済みの問題は次問／完走を一度だけ確定してからpausedまたはfinishedへ正規化する。
- **九番固有:** 基本2くるり9問から保存可能な順序と対称変換を作る。上部は学園方式にし、タイマー左に大きい「あと○くるりでクリア」、右寄りかつpauseと上下中央のタイマー、コツ位置の同サイズpauseを置く。「いま○くるり」、やり直す、一手戻す、次の1手、コツを見るは出さない。候補棒は1〜3問目3本、4〜6問目4本、7〜9問目5本で、正解候補を含め、誤った棒は同じ問題中だけ落下・選択不可にする。
- **連続出題と終点:** 通常クリアダイアログを挟まず、短い共通クリア演出から次問へ進む。九番完走時は起動元と記録を含む結果descriptorを一度だけ保存する。P7aでは後続コースの進捗・称号・報酬を付与せず、P7cが同じdescriptorを使って試験終点へ接続できる安定状態で止める。
- **配置:** 自然な `speed` / `速解き` / `timer` / `時計` / `pause` / `一時停止` / `九番` の検索から、セッションpolicy、時計、保存正規化、九番UI、候補棒、次問確定へ到達できる近接範囲に置く。共通Board Engine、棒、pointer、回転、クリア演出は複製しない。
- **完了条件:** 九番を開始待ちから9問完走できる。pause、再開、再スタート、誤棒、問題間遷移が動き、起動元を保持したfinished状態になる。F5とhidden復帰のどちらも同じ問題・順序・盤面・累積時間をpausedで復元し、再開前に時計が動かない。
- **検証:** 構文、`git diff --check`、起動時自己検査、コンソール例外なし。PCと360pxで1・4・7・9問目の候補数と上部UIを確認する。開始待ち、running、手動pause、誤棒後、advancing、9問目確定前後でリロードし、時間・問題・落下棒・完走が重複しないことを確認する。E2Eは作らない。
- **コミット境界:** `rebuild(p7a): add speed core and nine challenge`。
- **停止:** P6cが未コミット、時計の所有者がDOMと保存stateに分かれる、復元で自動再開する、壁時計時間を加算する、九番専用Board Engine／pointer処理を作る、または試験進捗をP7aで先行付与する場合。

#### P7b. 十八番・二十七番・七十三番

- **仕様:** `SPD-001`、`SPD-002` の種別、問題順、UI差、最短クリア数、通常完走記録部分。試験合格、次コースへの合流、金色盤面報酬はP7cが所有する。
- **実装対象:** `index.rebuild.html` 一枚だけ。`index.html` は問題集合、並び方、表示差、操作差の観測元として参照し、変更しない。
- **開始条件:** P7aが独立コミット `7029a7a`（`rebuild(p7a): add speed core and nine challenge`）として確定し、作業ツリーが空であることを最初に確認する。P7aの未コミット修正が残っている場合はP7bを混ぜずに停止する。
- **P7bに含めるもの:** 十八番、二十七番、七十三番の固定プールと順序、4種別の表示・操作policy、最短クリア数、variant別上位記録、解放判定、最小の速解き種別選択、P6bの十八番／二十七番要求からの開始、保存復元、開発用入口、自己検査。
- **P7bに含めないもの:** 試験合格フラグ、通常コース進捗の変更、試験後の次コース遷移、金色盤面、名人称号、二十七番完走フラグの付与。これらはfinished descriptorをP7cへ渡せる状態までに留める。

##### P7b-1. 4種別の正本とpolicy

問題プール、順序、盤面の見せ方、HUD、操作、解放を別々の条件分岐へ散らさず、`speed variant` / `速解き種別` の近くに一つのvariant定義として置く。九番の既存挙動も同じ表で明示し、P7b追加時の回帰を検出できるようにする。

| variant | 問題プール・件数 | 順序 | 盤面view | 残り表示 | Undo | Restart | pause位置 | 候補棒 | 通常起動の解放 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 九番 | 基本2くるり9問・9問 | 9問を重複なくshuffle | 問題ごとに12対称から保存済みview | 表示 | 非表示 | 非表示 | タイマー横 | 3／4／5本 | 発展クラス一周目完了 |
| 十八番 | 修行上巻9問＋中巻9問・18問 | 18問を重複なくshuffle | 旧版 `SPEED_BOARD_VIEW` と等価な固定view | 表示 | 表示 | 表示 | 下部操作列 | なし・6本すべて操作可 | 修行27問完了 |
| 二十七番 | 悟り固定73問から27問・27問 | 73問から重複なしでsample | 同じ固定view | 非表示 | 表示 | 非表示 | タイマー横 | なし・6本すべて操作可 | 名人45問完了 |
| 七十三番 | 悟り固定73問・73問 | 1手1問を先頭、2手9問をshuffle、その後は5問単位の難度帯内だけshuffle | 同じ固定view | 非表示 | 非表示 | 非表示 | タイマー横 | なし・6本すべて操作可 | 悟り一周目完了 |

- variant定義には少なくとも `id`、表示名、`routeId`、問題プール、出題数、`orderPolicy`、`viewPolicy`、残り表示、Undo、Restart、pause配置、候補棒、記録key、解放selectorを持たせる。renderer、入力、保存側でvariant名を列挙し直さない。
- 固定viewは旧版の「盤面を60度左へ回した見え方」と等価な `VIEW_SYMMETRIES` の一要素を一度だけ特定し、policyから参照する。固定view用に盤面状態や棒対応を別実装しない。
- 九番だけの候補棒、誤棒落下、学園型HUDは `candidatePolicy` / UI policyから有効化する。他3種へ空要素や九番用classを出さない。
- 十八番のRestartは「現在の問題だけを初期状態へ戻し、順序と経過時間は維持する」。二十七番・七十三番には出さない。Undoはvariant定義が許可した場合だけ共通履歴へ委譲する。

##### P7b-2. 固定問題ID、順序、保存可能な出題表

- 十八番は `TRAINING_UPPER_PUZZLES`、`TRAINING_MIDDLE_PUZZLES` の順に参照して18件の速解き用固定エントリを作る。各エントリは `speed-eighteen-001`〜`018`、`sourcePuzzleId`、state、parを持ち、下巻を混ぜない。
- 二十七番と七十三番は、既存 `SATORI_PUZZLES` 73問から一つの不変な速解き悟りプールを作る。`speed-satori-001`〜`073` と `sourcePuzzleId` を持たせ、同じ73状態をvariantごとに複写しない。solverの列挙順から実行時に問題表を作らない。
- `speedOrderFromSeed` をvariantの `orderPolicy` と `total` に従う決定的な生成へ拡張する。二十七番はプール73件に対してorder 27件となるため、「order長はpool長」というP7aの正規化前提を外し、期待件数、index範囲、重複禁止をpolicyから検証する。
- 七十三番の難度帯は問題番号やparを実行時推測せず、固定プールの範囲として定義する。先頭1件、次の9件、その後の5件単位の各帯を越えて混ぜない。
- order itemは `baseIndex` と `symmetryIndex` を保存し、同じseedから同じ順序・viewを再生成できるようにする。ただし有効な保存orderがある復元では再生成せず、そのorderを正本にする。
- 九番だけ各問のviewをseedから選ぶ。十八番・二十七番・七十三番は全order itemへ同じ固定viewを保存する。開始時とF5復元時で見た目を変えない。
- session／result契約の変更時は `APP_SCHEMA_VERSION` と `APP_CATALOG_VERSION` を一度だけ更新し、旧形と新形を半端に受け入れる互換分岐を残さない。

##### P7b-3. 共通セッションと種別ごとのUI・操作

- 4種別すべてをP7aの `awaiting-start`、`running`、`paused`、`advancing`、`finished`、時計、visibility/pagehide、連続出題、保存復元へ接続する。variant別の時計、到着演出、クリア演出、Board Engine、pointer処理を作らない。
- 開始待ちは解決済み盤面と開始操作を表示し、開始後だけ第1問と時計を動かす。問題間は通常クリアダイアログを挟まず、P7aと同じ短い共通クリア演出から次問へ進む。
- 上部HUDと下部操作列はvariant policyだけから描画する。九番の見た目を維持しつつ、十八番は残り手数、Undo、Restart、下部pause、二十七番はUndoとタイマー横pause、七十三番はタイマー横pauseだけを出す。「次の1手を見る」と「コツを見る」は全速解きで出さない。
- 非九番では `fallenAxes`、`candidateAxes`、`candidateSignature` を常に空へ正規化し、候補棒の生成・落下判定を呼ばない。六本の共通棒と既存の当たり判定をそのまま使う。
- PCと360pxの両方で、タイマー、残り表示、pause、Undo、Restartが盤面や互いに重ならないことを確認する。P8の全設定・装飾仕上げを先取りしない。

##### P7b-4. 最短クリア数、Restart、Undo、上位記録

- sessionは `optimalClears` と `currentPuzzleRestarted` を所有する。問題を `history.length === puzzle.par` かつ同問題でRestartしていない状態で初めて確定した場合だけ、最短クリア数を1増やす。
- 十八番でRestartした場合は `currentPuzzleRestarted = true` とし、その問題を最短手数で解き直しても最短クリアへ数えない。次問開始時にだけfalseへ戻す。Undoは履歴を戻すだけで、Restart扱いにはしない。
- 最終手の確定コマンドで、最短判定、`optimalClears` 更新、`advancing` または `finished` への遷移を同時に保存する。演出callbackやrendererから加算せず、advancing中のF5で二重加算しない。
- P7aの曖昧な `completedCount` は、P7bで `optimalClears`、`total`、`elapsedMilliseconds`、variant、originが明確なresult契約へ正規化する。互換性維持のためだけの別名は残さない。
- variantごとに完走時間と最短クリア数を含む上位3記録を保存する。同記録の並び順は「経過時間が短い順」を第一基準、同時間なら最短クリア数が多い順とし、一度のfinished確定で一度だけ登録する。

##### P7b-5. 解放、入口、起動元、P7cへ渡す終点

- 解放はDOMやボタン有無ではなく、純粋なselectorが進捗から判定する。九番は発展一周目完了、十八番は修行27問完了、二十七番は名人45問完了、七十三番は既存 `selectSatori73Unlocked(progress)` を正本とする。
- 通常起動用に最小の「速解き種別選択」を用意し、解放済みvariantだけを表示する。これはP7bの4種別を実際に選べる入口であり、P8の全体メニュー、設定、装飾仕上げまでは作らない。直接関数を呼んでも未解放variantを通常起動できないよう状態側でも拒否する。
- 起動元は `academy-trial`、`training-trial`、`mastery-trial`、`menu`、`debug` を保存可能な列挙として持つ。P6a/P6bの十八番要求・二十七番要求ダイアログは、それぞれ正規factoryから対応variantを試験起動する。九番の既存試験起動を壊さない。
- finished descriptorは variant、origin、経過時間、最短クリア数、総問題数、完走済みであること、元routeを持つ。メニュー起動では汎用完走結果を表示し、試験起動ではP7cが遷移先を決められる安定状態で止める。
- P7bでは `training9`、`training18`、`mastery27` の合格フラグ、通常コース進捗、報酬、テーマ、称号を一切変更しない。試験起動を完走してもP7c未実装の説明と結果から戻れるだけにする。

##### P7b-6. 中断・復元・破損時fallback

- 4種別とも、開始待ちは開始待ち、runningだった保存値はpaused、手動pauseはpaused、advancingは次問またはfinishedへ一度だけ正規化する。復元時に壁時計との差分を加算しない。
- order、currentIndex、盤面、履歴、経過時間、候補棒状態、最短クリア数、Restart済み、originを一つのspeed sessionとして保存する。保存orderを別の乱数順へ差し替えない。
- 二十七番の27件sample、七十三番の難度帯順、固定view、originのどれかが壊れた保存値は、その一部だけを推測修復せず、既存の安全な速解きfallbackへ落とす。別コースの進捗や報酬は変更しない。
- 完走resultと上位記録は復元時に再登録しない。finished画面のリロード、戻る操作、同じdescriptorの再描画が記録件数を増やさないことを確認する。

##### P7b-7. 開発用入口、自己検査、実行順

- 開発用入口は十八番・二十七番・七十三番それぞれについて、開始待ち、中間問題、最終問題直前、finishedを正規factoryから作る。公開解放を迂回する特別な盤面・時計処理は作らず、`debug` originだけを差にする。
- 自己検査は、4variantのID一意性、プール件数、出題数、固定source対応、solver最短距離、seed再現性、order重複禁止、七十三番の帯境界、view policy、UI policy、解放selector、origin、result正規化を確認する。
- 実装順は、(1) 固定プールとvariant policy、(2) policy駆動orderと正規化、(3) UI・操作差、(4) 最短クリア数と記録、(5) 解放・種別選択・試験入口、(6) 開発用入口と自己検査、(7) 手動比較とする。前段の自己検査が通る前に次の段へ進まない。

- **完了条件:** 4variantがP7aの共通セッション、時計、盤面、入力、演出で開始・中断・復元・完走できる。問題数、順序、view、UI、操作、解放、最短クリア数、上位記録が上表どおりで、未解放variantは状態判定から開始を拒否する。P7c所有の進捗と報酬は変化しない。
- **検証:** 構文、`git diff --check`、起動時自己検査、コンソール例外のない最小起動確認を行う。各variantの先頭・中間・最終、開始待ち、running、pause、hidden、途中F5、advancing、finishedを確認する。最短成功、余分な手、Undo後成功、十八番Restart後成功、最終手直後F5で最短数と記録が重複しないことを確認する。九番1・4・7問目の候補数とHUD、十八番の残り・Undo・Restart、二十七番のUndo、七十三番の操作列をPCと360pxで比較する。E2Eは作らない。
- **自然検索レビュー:** 別文脈のAIが検索語を指定されずに「十八番のやり直し」「二十七番の問題抽選」「七十三番の順番」「速解きの最短クリア数」「速解きの解放条件」を調べ、自然な日本語または英語の `rg` からvariant定義と直近の実装・壊してはいけない契約へ到達できることを確認する。検索専用の不自然なキーワード埋め込みはしない。
- **コミット境界:** `rebuild(p7b): add remaining speed variants`。P7cを同じコミットへ含めない。
- **停止:** 問題プールをsolver列挙順から暗黙生成する、variantごとに時計・保存・次問・盤面入力を複製する、固定viewを盤面状態の手修正で再現する、保存復元で順序を再抽選する、UI差をrenderer各所のvariant名分岐へ散らす、またはP7c前に試験合格・次コース進捗・金色報酬を確定する場合。

#### P7c. 試験合流、記録、金色盤面報酬

- **仕様:** `SPD-001`、`SPD-002` の試験終点、`REWARD-001` の名人報酬部分、`FLOW-002`。
- **実装対象:** `index.rebuild.html` 一枚だけ。`index.html` は合格ダイアログ、称号、金色配色、次コースへの接続を確認するために参照し、変更しない。
- **開始条件:** P7bが独立コミット `2604b99`（`rebuild(p7b): add remaining speed variants`）として確定し、作業ツリーが空であることを最初に確認する。P7bの未コミット修正が残っている場合はP7cを混ぜずに停止する。
- **P7cに含めるもの:** 三つの試験finishedの合格確定、合格ダイアログ、次コース入口への接続、試験合格フラグ、二十七番試験の金色盤面報酬、保存復元、開発用入口、自己検査。
- **P7cに含めないもの:** 自由モード、自作モード、全体メニュー、テーマ選択UI、七十三番の追加報酬、二周目の新報酬。P7bの問題順、時計、操作差、上位記録方式も変更しない。

##### P7c-1. 試験終点policyと合格条件

`SPEED_TRIAL_POLICIES` を試験開始だけでなく完走判定にも使い、origin、variant、元route、試験合格ID、次route、合格表示を一か所で対応付ける。別のswitchへ同じ対応表を複製しない。

| origin | variant | 元route | 保存する合格ID | 次の入口 | 合格後の既存案内 |
| --- | --- | --- | --- | --- | --- |
| `academy-trial` | `nine` | 発展8問目・一周目 | `training9` | 修行上巻1問目 | `training-welcome` |
| `training-trial` | `eighteen` | 修行下巻9問目・一周目 | `training18` | 名人・序1問目 | `mastery-welcome` |
| `mastery-trial` | `twentySeven` | 名人・急15問目・一周目 | `mastery27` | 悟り1問目 | `satori-welcome` |

- 合格は最短クリア数に関係なく、対応variantの規定問題数を完走した正常なfinished resultで確定する。`optimalClears` は結果表示と記録に残すが、試験の合否へ使わない。
- 合格判定は、正規化済みfinished session、`result.completed === true`、resultとsessionのvariant／origin／total／returnRoute一致、元routeの同一周回完了をすべて確認する。DOM、表示中のダイアログ、ボタン有無は見ない。
- `menu` と `debug` originは常に汎用完走であり、同じvariantを完走しても試験合格へ変換しない。originとvariantの組合せ違い、二周目のreturnRoute、未完了の元コースは試験合格として受け入れない。
- 速解きvariantの通常選択解放は既存の進捗selectorから導出したままにし、重複するunlock booleanを追加しない。試験ゲートだけが `completedTrialIds` を正本にする。

##### P7c-2. finished確定と冪等コマンド

- finished resultから `{ kind:'menu-result' }` または `{ kind:'trial-pass', completedTrialId, nextNavigation, welcomeDialog, grantsMasterGold }` を返す純粋なdestination factoryを作る。行先の決定と状態変更を同じ関数へ混ぜない。
- 最終問の確定では、P7bの上位記録登録、finished session、試験合格ID、必要なら金色報酬を一つの次AppStateとして組み立て、`saveAppState`を一度だけ呼ぶ。クリア演出、renderer、ダイアログ描画から合格や報酬を書き込まない。
- 通常実行の `finishSpeedSession`、最終問advancing中のF5復元、hidden／pagehideによる安定化は、同じ試験完走finalizerを使う。経路ごとに合格処理を複製せず、どの経路でも記録と合格を一度だけ確定する。
- finished中は有効なactive sessionとresultを保持し、試験合格画面を再描画できる安定状態にする。合格IDは集合として追加し、同じfinalizerを二度適用してもID、上位記録、報酬が増えない。
- 試験合格画面の「次へ」でだけactive speed sessionを閉じ、destination factoryが示す次コース1問目へ移る。次route作成は既存の `makeTrainingEntryAppState`、`makeMasteryEntryAppState`、`makeSatoriEntryAppState` を通し、各コースの盤面factoryを複製しない。

##### P7c-3. 合格ダイアログと次コースへのチェイン

- 試験finishedは汎用の「速解き○番勝負 完走！」ではなく、旧版の節目に対応する合格ダイアログを出す。九番は「だるま学園 卒業！」、十八番は「だるま修行 修了！」、二十七番は「名人への道 制覇！」を主題とし、旧版の合格印・文言・ボタンの見た目と振る舞いを比較して取り込む。
- 合格印は九番「修了試験／速解き九番勝負／合格」、十八番「修行卒業試験／速解き十八番勝負／突破」、二十七番「免許皆伝／速解き二十七番勝負／制覇」を基準にする。汎用完走のタイム・最短数・上位記録UIを合格印と重ねない。
- 九番の次ボタンは修行上巻、十八番は名人・序、二十七番は悟りへ接続する。遷移後は既存の `training-welcome`、`mastery-welcome`、`satori-welcome` を表示し、開始案内を飛ばさない。
- 合格画面はfinished sessionから導出し、同内容のHTMLを保存しない。F5では同じ合格画面へ戻り、「次へ」を押した後のF5では次コース入口と既存welcomeへ戻る。
- P7bの「元のコースへ戻る」は試験finishedでは廃止し、汎用結果へ戻さない。通常のmenu完走は従来どおり「もう一度」「速解き一覧」を持ち、コース遷移を出さない。

##### P7c-4. 二十七番試験と金色盤面報酬

- 金色報酬の条件は、`mastery-trial` origin、`twentySeven` variant、一周目の名人45問完了を示す正規returnRoute、正常finished result、`masterGoldGranted === false` のすべてとする。名人45問を終えただけ、menuからの二十七番、debug完走、九番／十八番完走では付与しない。
- 初回だけ `rewards.masterGoldGranted = true` とし、`settings.theme = 'gold'`、`settings.themeChosen = false` を同じ試験完走コマンドで確定する。layout、`layoutChosen`、悟り報酬は変更しない。
- すでに `masterGoldGranted === true` なら、現在のthemeと `themeChosen` を上書きしない。ユーザーが後から標準色等を明示選択できるP8の契約を壊さない。
- `body[data-w7-board-theme="gold"]` で本編盤面へ旧版相当の金色を適用する。基準色は起きたパネル `#F6DE93`、寝たパネル `#D7B75F`、枠 `#C89C35` とし、論理盤面、棒、当たり判定、回転順、ダイアログ内の通常見本へ影響させない。
- 二十七番合格画面では金色になった盤面を確認できるようにする。報酬演出が必要でも、演出は確定済みstateを見せるだけとし、演出完了を報酬保存の条件にしない。

##### P7c-5. 保存復元と破損時fallback

- 正常な試験finishedは、session result、上位記録、合格ID、報酬の整合が取れた一つの安定状態として復元する。最終問advancingの保存はP7bの規則でfinishedへ正規化したうえで、同じfinalizerにより合格状態へ一度だけ進める。
- 合格後の次コースwelcome descriptorは、route先頭、未解決の初期盤面、対応する合格ID、必要な前コース進捗が揃う場合だけ復元する。壊れた合格表示を別routeへ重ねない。
- origin、variant、returnRoute、resultのいずれかが不正なら、合格IDや金色報酬を推測付与しない。既存の安全なspeed fallback、または元コースの要求ダイアログへ戻し、他コースの進捗・既存報酬・設定は保持する。
- 完走記録はP7bのfinished確定で一度だけ登録し、P7cの合格確定、F5、次へ、合格画面再描画では追加しない。保存shapeを変えない限りschema／catalog versionを不要に上げない。

##### P7c-6. 開発用入口、自己検査、実行順

- 開発用入口は九番・十八番・二十七番それぞれの「試験最終問直前」「試験合格finished」「次コースwelcome」を正規factoryから作る。二十七番は金色未獲得の初回合格と、獲得済み・theme明示選択済みの再適用防止を確認できる入口も用意する。
- 自己検査は三つのorigin／variant対応、元routeと一周目制約、最短数に依存しない合格、合格IDの一意追加、次route、menu/debug非合格、通常二十七番の非報酬、初回金色付与、再適用時の設定保持、advancing復元、合格finished復元、次へ後welcome復元を確認する。
- 実装順は、(1) trial destination policy、(2) 冪等finalizer、(3) runtime／F5／hiddenの三経路統合、(4) 合格ダイアログと次コース操作、(5) 金色CSSと報酬表示、(6) 保存正規化、(7) 開発用入口と自己検査、(8) PC／360px手動比較とする。

- **完了条件:** 九番試験が修行、十八番試験が名人、二十七番試験が悟りへ一度だけ合流する。通常起動とdebug起動は同じvariantを完走してもコース進捗・合格ID・報酬を変更しない。二十七番試験の初回完走だけが金色盤面を獲得して即時適用し、再完走は現在設定を上書きしない。三つの合格finishedと次コースwelcomeがF5後も同じ状態へ戻る。
- **検証:** 各試験の最終問確定前、advancing保存、finished合格表示、次へ直後、welcome表示中でリロードする。完走記録、合格ID、次route、金色報酬が欠落・二重付与されないことを確認する。`optimalClears = 0` でも試験完走なら合格し、menu起動の九番・十八番・二十七番、debug完走、未完了／不一致returnRouteでは非合格であることを確認する。二十七番初回、報酬獲得済み、標準theme明示選択済みの三状態でtheme挙動を確認する。構文、`git diff --check`、起動時自己検査、コンソール例外なしの最小起動確認を行い、E2Eは作らない。
- **自然検索レビュー:** 別文脈のAIが検索語を指定されずに「九番合格後に修行へ進む」「十八番の試験合格」「二十七番で金色盤面をもらう」「速解き試験のリロード」を調べ、自然な日本語または英語の `rg` からtrial policy、finished finalizer、合格ダイアログ、次route、報酬、保存復元へ辿れることを確認する。処理を近づけた結果、P7bの時計・問題順・通常結果から遠くなるなら、無理に一塊へせず、名前付きの純粋な境界一つで接続する。
- **コミット境界:** `rebuild(p7c): connect speed trials and master reward`。P7dへ進まず、P7bの完成済みセッション基盤を同じコミットで再設計しない。
- **停止:** 画面要素やダイアログ表示をゲート判定に使う、originとreturnRouteの照合なしに合格させる、runtimeと復元で別々の合格処理を持つ、演出終了時に進捗・報酬を付与する、通常完走やdebug完走で試験合格になる、報酬再適用でユーザー設定を上書きする、または次コース用の盤面／進行factoryを複製する場合。

#### P7d. 自由モード

- **仕様:** `FREE-001`、`OPEN-008`。
- **実装対象:** `index.rebuild.html` 一枚だけ。`index.html` は自由問題の文言、表示、別問題への遷移、離脱／再入場の振る舞いを確認するために参照し、変更しない。
- **開始条件:** P7cが独立コミット `530103d`（`rebuild(p7c): connect speed trials and master reward`）として確定し、作業ツリーにP7cの未コミット修正が残っていないことを確認する。残っている場合はP7dを混ぜずに停止する。
- **P7dに含めるもの:** 距離2〜4の自由問題生成、自由route、共通盤面での操作、Undo／Restart、クリアと次の自由問題、表示中F5、離脱時保存、再入場復元、破損fallback、開発用入口、自己検査。
- **P7dに含めないもの:** 自作問題、P8の完成版メニュー／設定画面、通常コースの進行UI再設計、自由問題の記録・称号・報酬、速解きの時計・記録・試験合流。自由問題を通常コースの問題カタログや完了集合へ追加しない。

##### P7d-1. 自由routeとセッション契約

- `free mode` / `自由問題` の状態所有者を一か所に置き、`navigation.mode = 'free'`、`routeId = 'free'`、`courseId = 'free'`、`sectionId = 'free'` の専用routeを定義する。自由問題の `puzzleId` は初期状態から安定して再構成できる名前にし、通常コースのID空間と混ぜない。
- 自由セッションは一つの `FreeSessionDescriptor` で表す。最低限、初期状態、最短距離、初期セル、現在セル、検証可能な回転履歴、`clearCommitted` を持つ。現在表示中の自由セッションと、離脱後に保持する `savedSession` は同じdescriptor／同じ正規化関数を使い、片方だけにしかない盤面形式を作らない。
- `AppState.free` は自由固有状態だけを所有する。現在routeが自由ならactive側を使い、他routeではactive側を空にして `savedSession` だけを保持する。自由へ入る直前の戻り先はactive側の付帯情報として分離し、古いsavedSessionの戻り先で現在の入口を上書きしない。
- 保存shapeが増えるため `APP_SCHEMA_VERSION` をP7dで一度だけ更新する。`APP_CATALOG_VERSION` は固定問題カタログを変更しない限り上げない。通常コース、速解き、報酬、設定の既存フィールドはそのまま保持する。

##### P7d-2. 距離2〜4の問題生成

- `SOLVER.byDepth[2]`、`[3]`、`[4]` を自由問題の候補に使い、選んだstateの距離を `SOLVER.distance` で再確認する。通常コースと違い自由問題はランダム生成が仕様なので、この用途に限ってsolverの深さ別集合を使用してよい。
- 直前の有効な自由初期状態を覚え、候補が複数ある場合は同じstateを再選択しない。回数上限なしの再抽選ループにはせず、直前state以外の候補から一度選ぶ。候補が一つしかなければ同じstateを許可する。
- stateから `{ puzzleId, state, par }` を作る純粋なfactoryを用意し、盤面の初期セル、残り手数表示、Restart、保存検証が同じ `par` を使う。現在セルやDOMから最短距離を推測して初期値にしない。
- 新規問題の決定後は通常問題と同じ `makeBoardState` 相当、共通到着演出、共通棒、pointer入力、回転、Undo、Restartを使う。自由専用Board Engine、solver、回転処理は作らない。

##### P7d-3. 開始、離脱、再入場

- `intentEnterFreeMode` は現在の通常routeを安全な戻り先として保持し、有効な `savedSession` があればそのセッションをactiveへコピーして復元する。保存がない、または正規化で破棄された場合だけ新規問題を生成する。
- 自由表示中のF5はactive descriptorと盤面・履歴を照合し、同じ問題、同じ現在セル、同じ履歴、同じクリア状態へ戻す。到着、回転、Undo、Restart、クリア演出の途中フレームは保存せず、それぞれの直前または直後の安定状態へ正規化する。
- `leaveFreeMode` は共通の一つの境界とし、現在の有効なactive descriptorを `savedSession` へコピーしてからactiveを空にし、検証済みの戻り先へ移る。将来P8のメニュー、開発用移動、通常コースへの復帰はいずれもこの境界、または同じ保存commandを通す。
- 別モード表示中は `savedSession` が存在しても `navigation` を変更しない。別モードでF5してもそのrouteを復元し、後から自由へ明示的に入った時だけsavedSessionを使う。速解きactive session、通常進捗、報酬、設定を自由の出入りで消さない。

##### P7d-4. 操作、クリア、次の問題

- 自由routeでは、通常盤面と同じ「いま○くるり」「あと○くるり」、Undo、Restartを表示する。修行ヒント、名人の残り確認、速解きHUD、学園の候補棒絞り込み、応用ガイドは出さない。
- Undo、Restart、回転確定の各安定境界で、共通 `AppState.board` とactive free descriptorを同じcommandで同期する。描画側や `beforeunload` だけに保存責務を置かない。
- 解決時は通常のクリア演出を使うが、`withCompletedPuzzleId`、コース境界、報酬、速解き記録を呼ばない。自由専用の冪等clear commandが `clearCommitted` と自由クリアdescriptorを保存してから演出を開始する。
- クリアダイアログは旧版と同じ「全員起きました！」と「別の問題」操作を基本にする。「別の問題」は前問と異なる初期状態を優先して新規自由セッションへ置き換え、共通到着演出で開始する。クリア直後にF5した場合は同じ解決済み盤面とクリアダイアログを復元し、自動で次問へ飛ばさない。

##### P7d-5. 保存検証と破損fallback

- `normalizeFreeSession` は初期stateが整数で距離2〜4、初期セルがstateのdecodeと一致、`par`がsolver距離と一致、現在セルが7セルの有効向き、履歴が `validMoveHistory` で初期セルから現在セルまで完全再生できることを確認する。
- `clearCommitted` は「現在セルが解決済み」と一致する場合だけ受け入れる。未解決なのにclear済み、解決済みなのに履歴とclear状態が矛盾する値から、通常コースのクリアや自由報酬を推測しない。
- 現在routeが自由でactiveが壊れていれば、進捗・速解き・報酬・設定と有効なsavedSessionを保持したまま、新しい自由問題へ安全に置き換える。現在routeが自由以外なら、壊れたactive／savedだけを `null` にし、そのrouteと盤面を維持する。
- 保存復元の自由分岐は通常コースの `navigationFromPersistentState` と盤面復元の途中へ小条件を散らさず、`normalizePersistentFreeAppState` のような名前付き境界へ集約する。汎用factoryがrouteを変える時も `free.savedSession` を落とさないよう、保存状態の引き継ぎ箇所を一度棚卸しする。

##### P7d-6. 開発用入口、自己検査、実行順

- P8の完成版メニュー前でも手動確認できるよう、一番下の開発用ボタンに「自由を新規開始」「保存済み自由へ再入場」「自由を途中状態にする」「自由クリア直後」を追加する。開発用入口も正規factory、正規保存検証、正規clear commandを使う。
- 自己検査は距離2／3／4生成、直前state回避、履歴再生、Undo後、Restart後、クリア済み復元、離脱コピー、別route保持、再入場、破損active fallback、破損saved破棄、通常進捗・速解き・報酬の非変更を確認する。
- 実装順は、(1) free state／descriptor／正規化、(2) free navigationとpuzzle factory、(3) 新規生成、(4) active AppStateへの投影、(5) 回転・Undo・Restart同期、(6) clear commandと別問題、(7) 離脱／再入場、(8) 永続化fallback、(9) 表示、開発用入口、自己検査、(10) PC／360px手動比較とする。

- **配置:** `free mode` / `自由モード` / `自由問題` / `saved free session` / `自由セッション復元` の自然検索から、descriptor、生成、保存、離脱、再入場、破損fallback、クリア後routeまで追えるようにする。共通盤面との接続だけは既存の入力／描画／クリア境界に置き、自由固有条件を汎用保存関数やrenderer各所へ重複させない。
- **完了条件:** 自由の途中盤面・初期盤面・履歴が、表示中F5、別モード往復、別モード移動後のF5と再入場のすべてで一致する。クリア直後は同じクリア画面を復元し、「別の問題」で新規状態へ進む。保存済み自由セッションは現在routeを奪わず、破損値は通常コースの状態を変えずに破棄または新規自由問題へ置換される。
- **検証:** 距離2／3／4それぞれで0手、複数手、Undo後、Restart後、クリア直後を確認する。自由表示中F5、別モード離脱、離脱先F5、再入場、active破損、saved破損を確認する。連続新規問題で直前状態回避が働くこと、自由クリアで通常進捗・試験ID・上位記録・報酬が変わらないことを確認する。構文、`git diff --check`、起動時自己検査、コンソール例外なしの最小起動確認を行い、E2Eは作らない。
- **自然検索レビュー:** 別文脈のAIが検索語を指定されずに「自由問題を作る」「自由モードを離れて戻る」「自由問題の途中でリロード」「壊れた自由セッション」を調べ、自然な日本語または英語の `rg` から問題生成、session descriptor、保存command、再入場、復元fallback、クリア後の別問題まで辿れることを確認する。近づけることで共通盤面の回転や通常コース保存から離れる処理は、無理に移動せず名前付き境界一つで接続する。
- **コミット境界:** `rebuild(p7d): add persistent free mode`。P7eへ進まず、自作編集、全体メニュー、設定UIを同じコミットへ入れない。
- **停止:** 保存済み自由セッションが起動routeを奪う、active sessionとsaved free sessionが異なる形式で二重実装される、DOMから盤面を復元する、自由クリアが通常進捗・記録・報酬を変更する、自由専用Board Engine／solverを作る、または汎用factoryの遷移でsavedSessionを失う場合。

#### P7e. 自作問題

- **仕様:** `CUSTOM-001`、`PERSIST-001`、`PERSIST-002`。チェックリストC5の「自作盤面の編集状態とプレイ状態」、C8の入力ロック、DのPC／スマホ・pointer・reduced motionを対象にする。
- **入力:** P7d確定コミット `2c8c330`、解決済み7枚盤面、既存solver、共通盤面・棒・pointer・回転・履歴・Undo・Restart・到着演出・クリア演出、P7dの自由セッション保存。`index.html` は旧版の表示・文言・振る舞いを確認する参照専用とし、実装対象は `index.rebuild.html` 一枚だけにする。
- **範囲:** 解決済み盤面から始める編集、7セルの向き変更、可解性／最短手数表示、プレイ開始拒否、有効盤面のプレイ、Undo、Restart、クリア後の編集復帰、編集／プレイ中のF5復元、破損保存fallback、開発用入口、自己検査までをP7eとする。P8の完成版メニュー、設定、翻訳拡張、自作問題の共有・文字列入出力・保存スロット・成績記録は含めない。

##### P7e-1. routeと自作状態の契約

- 自作画面は `navigation.mode='custom'` を正とし、`AppState.custom` に現在の自作セッションだけを持たせる。画面class、ボタン表示、メッセージ文言、DOM上のだるま角度を状態所有者にしない。
- 自作descriptorは `phase:'editing'|'playing'` を必須にする。`editing` は編集中の7セルと通常コースへ戻るための検証済み `returnNavigation`、`playing` は自作初期セル、初期state、solverで求めたpar、現在セル、履歴、clear状態、同じ戻り先を持つ。`editingBoard` のような独立booleanとmodeの組合せで同じ意味を二重表現しない。
- 自作へ明示的に入った時だけ、全セルが起きた解決済み盤面で新規editorを作る。別routeに保存されたcustom descriptorだけを理由に起動routeを自作へ変えない。P7dの `free.activeSession` / `free.savedSession`、速解きactive session、通常進捗、報酬、設定を自作の出入りで消さない。
- 自作は「前回の自作を後日再開する保存スロット」にはしない。自作route表示中の安定状態だけをF5復元し、通常コースへ明示的に戻った時はactive customを閉じる。将来この方針を変える場合は、P7dのsaved free sessionとは別仕様として決定する。
- 永続schemaはP7eで一度だけ更新する。旧schemaはcustomなしとして移行し、既存の通常／速解き／自由／報酬を保持する。汎用AppState factoryを棚卸しし、routeを作り直す経路でもcustom以外の既存状態を欠落させない。

##### P7e-2. 盤面編集と即時判定

- 編集中は棒と通常回転を入力対象にせず、盤面セルをクリック／タップした時だけ、そのセルの向きを `0→1→2→0` と一段ずつ切り替える。セル判定は共通のpointer座標変換を使い、パネル外、到着演出中、ダイアログ表示中、別pointerの多重入力では編集しない。
- 編集操作は通常プレイのmove historyへ入れない。1セル変更commandがdescriptor、共通 `AppState.board`、表示を同じ安定境界で更新し、その直後に既存solverの距離を再計算する。編集用の盤面エンジン、可解表、手作業の形判定は作らない。
- solver距離が未到達なら「この配置は解けません。ダルマをタップして向きを変えてください」と表示し、最短手数欄を `—`、開始ボタンをdisabledにする。距離0なら「全員起きています。ダルマをタップして向きを変えてください」と理由を表示して開始を拒否する。距離1以上なら「ダルマをタップして向きを変更　現在の最短は○手」と表示し、その距離をparとして開始可能にする。
- 編集画面のやり直しは、7セルをすべて起きた状態へ戻して判定を更新する。Undo、ヒント、残り手数、通常の「あと○くるり」、候補棒、応用ガイド、速解きHUD、通常クリア判定は出さない。編集メッセージと「この盤面で遊ぶ」は盤面を隠さず、PC幅と360pxでタップ可能な位置に置く。

##### P7e-3. 有効盤面から共通プレイへ変換

- 「この盤面で遊ぶ」は押下時にも現在セルをsolverで再検証する。距離1以上の場合だけ、その瞬間の編集セルを不変の `initialCells` / `initialState`、距離を `par` としたplaying descriptorへ一回で変換し、履歴を空、clearをfalseにして共通盤面へ投影する。判定後にDOMから初期盤面を読み直さない。
- プレイ開始後は共通の棒、pointer入力、連続回転、確定／巻き戻り、Undo、Restart、到着演出、クリア演出を使う。Restartは解決済み盤面ではなく、その自作セッションの `initialCells` とparへ戻す。UndoとRestart後もplaying descriptorと共通boardを同じcommandで同期する。
- プレイ中は通常問題と同じ「いま○くるり」「あと○くるり」を表示し、自作編集メッセージを隠す。学園候補棒、応用対象ガイド、修行／名人ヒント、速解き時計・候補棒は有効にしない。
- 自作クリアは専用の冪等clear commandでplaying descriptorを解決済み・clear済みにして保存してから、共通クリア演出と「全員起きました！」ダイアログを出す。通常コースの完了ID、解放、試験、上位記録、報酬、自由問題のactive／saved sessionを変更しない。
- クリアダイアログの次操作は「盤面をつくる」に戻し、新しい解決済みeditorを作る。プレイ中に明示的に編集へ戻る操作も同じeditor生成境界を通し、途中盤面を暗黙に編集素材へ変換しない。クリア直後のF5では先に同じ解決済み盤面とクリアダイアログを復元し、勝手にeditorへ進めない。

##### P7e-4. 保存復元と破損fallback

- `normalizeCustomSession` はphaseごとに検証する。editingは7セルが有効な向きであることを確認し、解決済み・可解・到達不能のどれも正当な編集状態として受け入れる。playingは初期セルと初期stateが一致し、solver距離が1以上、parがその距離と一致し、履歴を初期セルから完全再生して現在セルへ到達できることを確認する。
- playingのclear状態は現在セルが解決済みである場合だけ受け入れる。未解決なのにclear済み、初期盤面が解決済み／到達不能、履歴と現在セルが不一致の保存値からクリアや進捗を推測しない。
- 編集中F5は同じ7セル、可解性、最短手数、開始可否へ戻す。プレイ中F5は同じ初期盤面、par、現在セル、履歴、Undo可否へ戻す。到着、回転、Undo、Restart、クリア演出の途中フレームは保存せず、それぞれ直前または直後の安定状態へ正規化する。
- 現在routeがcustomでdescriptorが壊れていれば、通常進捗、速解き、自由active／saved、報酬、設定を保持したまま、新しい解決済みeditorへ安全に置換する。custom以外のrouteに不要なcustom値が残っていればcustomだけを空にし、現在routeを奪わない。
- 速解きの進行中は自作へ直接遷移させない。P8で停止確認を実装するまでは、既存の速解きactive sessionを暗黙pause／破棄するのではなく、入口を拒否する。通常／自由から入る時はそれぞれ正規の離脱境界を通し、特に自由のsavedSessionを保持する。

##### P7e-5. 表示、開発用入口、自然検索

- 自作編集と自作プレイで見出し、ステージ表示、操作列を明示的に切り替える。編集は「自作問題をつくる」と最短手数、プレイは「自作問題」といま／あと手数を表示する。編集セルにはbutton相当のkeyboard操作と状態が伝わるaria-labelを持たせ、マウスだけに依存しない。
- P8の完成版メニュー前でも確認できるよう、一番下の開発用入口に「自作編集」「距離1」「距離4」「解けない配置」「自作プレイ途中」「自作クリア直後」を追加する。入口はテスト専用の状態代入ではなく、正規factory、編集command、プレイ変換、clear commandを通す。
- `custom board` / `自作問題` / `board maker` / `盤面編集` / `solvable` / `解ける判定` の自然検索から、descriptor、セル編集、solver判定、開始拒否、プレイ変換、Restart、保存復元、クリア後editorまで近接して追えるようにする。共通pointer・Board Engine・solver・clear表示との接続だけは既存の責務位置に残し、自作固有条件を汎用rendererや永続化関数へ点在させない。
- 「関連処理を近づけるために共通処理から遠ざける」場合は、処理本体を複製せず `enterCustomEditor`、`editCustomCell`、`startCustomPlay`、`normalizeCustomSession`、`completeCustomBoard` のような名前付き境界で一度だけ接続する。日本語コメントは検索用タグとして大量に挿入せず、その境界の意図と壊してはいけない契約を説明するために使う。

##### P7e-6. 自己検査と実行順

- 自己検査は、解決済み、距離1／2／3／4、到達不能の判定、開始可否、セル向き循環、初期盤面固定、履歴再生、Undo後、Restart後、編集F5、プレイF5、クリア済みF5、破損editing／playing fallback、通常進捗・速解き・自由・報酬の非変更を確認する。
- 実装順は、(1) custom descriptor／schema／正規化、(2) custom navigationとreturn先、(3) 解決済みeditor生成、(4) セル編集とsolver表示、(5) プレイ開始変換、(6) 共通回転・Undo・Restartとの同期、(7) custom clear commandと編集復帰、(8) F5復元・破損fallback、(9) 表示・keyboard・開発用入口、(10) 自己検査とPC／360px手動比較とする。

- **完了条件:** 編集→即時判定→有効盤面のプレイ→Undo／Restart→クリア→編集復帰が通る。解決済みと到達不能は理由付きで開始できない。編集／プレイ／クリア直後をF5して同じ安定状態へ戻り、自作を離れて通常コースまたは保存済み自由へ戻っても問題・進捗・速解き・自由セッション・記録・報酬を汚さない。
- **検証:** 解決済み、距離1〜4、到達不能の代表状態で編集、開始可否、編集連打、盤面外入力、keyboard、開始直後、複数手、Undo、Restart、クリア後を確認する。各phaseでF5し、破損editing／playingを注入してfallbackを確認する。通常問題、九番pause、自由の表示中F5／離脱後再入場、二十七番と報酬に回帰がないことを確認する。構文、`git diff --check`、起動時自己検査、コンソール例外なしの最小起動確認を行い、E2Eは作らない。
- **自然検索レビュー:** 別文脈のAIが検索語を指定されずに「自作盤面の向きを変える」「解けない自作問題」「自作を途中でリロード」「自作のやり直し」「自作クリア後に編集へ戻る」を調べ、自然な日本語または英語の `rg` から状態契約、編集command、solver判定、プレイ変換、Restart、保存fallback、クリア後routeまで辿れることを確認する。
- **コミット境界:** `rebuild(p7e): add custom board mode`。P8へ進まず、完成版メニュー、設定、言語追加、共有・入出力・保存スロットを同じコミットへ入れない。
- **停止:** 編集操作を通常の回転履歴へ混ぜる、可解性を見た目や手作業の条件分岐で判定する、編集とプレイをDOM／独立booleanで判別する、自作専用solver／Board Engineを作る、自作クリアで通常進捗・記録・報酬を付与する、自由savedSessionを失う、または破損customが別routeを奪う場合。

P7完了時にチェックリストC4・C5・C9を通し、別文脈のAIが検索語を指定されずに「速解きリロード後の時計」「九番の候補棒」「二十七番試験の金色報酬」「離脱後の自由盤面」「自作の解ける判定」を調べ、自然な日本語または英語の `rg` から各固有処理と壊してはいけない契約を周辺だけで説明できることを確認する。

### P8. 画面配置の是正、全ダイアログ、設定、文言、リセット（6段階）

- **仕様:** `UI-002`、`UI-003`、`PERSIST-003`、`REWARD-001` の設定切替部分、`OPEN-001`、`OPEN-002`、`OPEN-006`。
- **入力:** P7、確定した文言／言語範囲、設定一覧、ダイアログ復元マトリクス、装飾一致範囲。
- **作業:** 全対象ダイアログを単一Modal Managerへ載せ、focus、戻り先、重なり排除、復元rendererを揃える。言語、音、テーマ、配置、だるま色、通常／完全リセットを完成させる。金色・悟りテーマと30度傾斜配置は獲得済みの場合だけ選択肢を出し、明示選択を保存する。傾斜は論理セルを変えず盤面を左へ30度回す表示変換とし、だるまは右へ30度補正して正立させる。許可された文言・SVG・静的アートを移植する。
- **完了条件:** `OPEN-006` の各ダイアログで本文、ボタン、戻り先、focus、盤面アニメーションの復元結果が表通りであり、全設定とリセット境界が保存される。獲得済みテーマ／配置を後から切り替えて再読込でき、未獲得の選択肢は使用できない。
- **検証:** チェックリストC7・C9・D、モーダル中keyboard、入力欄中shortcut、音on/off、設定別のPC/スマホ表示、通常／完全リセット前後の保存差分を確認する。通常配置と30度傾斜配置で同じ手が同じ論理結果になることも確認する。
- **コミット境界:** `rebuild(p8): finish dialogs settings and content`。
- **停止:** `OPEN-001` / `002` / `006` が未決、復元が単なる `hidden=false`、複数モーダルが同時に開く経路がある場合。

#### P8の着手条件と共通方針

- **画面配置の是正はP8の必須作業とする（ユーザー確認済み）。** メニュー・言語・音の位置、タイトル・問題表示、盤面の位置と大きさ、残り手数、タイマー、操作ボタン、ダイアログ内の順序・文字サイズ・余白を旧版と比較して揃える。P7までに実装済みの画面も対象とする。「重ならず操作できる」だけでは完了にしない。採用済みの新版表示・演出は維持する。
- P8aで配置差分表を作る。各行にモード／画面状態、画面幅・高さ、旧版／新版の比較画像または観測内容、配置差、採用済み差分か是正対象か、担当段階、確認結果を残す。同じ画面サイズ・問題・進行状態・テーマ／配置で比較する。PCと360px幅のスマホを必須にし、デバッグボタンによる余白やスクロールを通常画面の配置と混同しない。
- 配置の完了判断は、位置・サイズ・並び・余白が旧版または明示的に採用した新版仕様に合っていることとする。確認できなかった画面は未確認のまま残す。装飾の完全一致範囲が未決でも、このユーザー確認済みの配置是正は進める。P9は回帰確認の段階とし、既知の配置差の修正を先送りしない。
- P7と保守性の局所改善は `54ac8af` を基準とする。各段階の前に作業ツリーと直前の完了範囲を確認する。
- 実装正本は `index.rebuild.html` 一枚。`index.html` は見た目・挙動・純粋コンテンツの参照用。付属HTMLは変更しない。計画・台帳・チェックリストの更新は実装とは区別して記録する。
- P8a〜P8fを順に実装・レビューし、各段階で止める。一括実装を標準にしない。P9の全体回帰と公開切替は含めない。
- 上記の「単一Modal Manager」は、既存の一つのbackdrop、`renderDialog`、dialog descriptorを使って一つだけ表示する意味とする。新しい汎用モーダル基盤へ全機能を移植する作業は要求しない。
- 機能名、UI文言、自然な英語の関数名から検索でき、開く・表示する・選ぶ・閉じるが近くで読める配置にする。盤面操作、回転、保存、速解き時計など既存共通処理は再利用する。全機能を一か所に寄せるための大量移動は行わない。
- 各段階の検証は構文、`git diff --check`、変更した状態契約の自己検査、可能な環境での最小起動確認を基本とする。E2Eの新設は不要。PC／360pxの確認項目を示し、ユーザー手動確認と自動確認を混同しない。

#### P8a. 共通画面の配置是正、残項目・復元契約の確定と全体メニュー

**目的:** デバッグボタンを使わず既存モードへ入り、元のプレイへ戻れる入口を完成させる。

- **最初に共通画面の配置を比較し、この段階で是正する。** チュートリアル、学園各クラス、修行・名人・悟り、速解き各種、自由、自作について、上部メニュー類、タイトル、問題表示、盤面、手数、タイマー、操作ボタン、応用の右上ガイドの位置・サイズ・間隔を比較する。通常配置と30度傾斜配置も確認する。既存の開始・クリア・失敗・一時停止ダイアログは、外枠・表示位置・共通余白も対象とし、内容固有の差分はP8b〜P8dへ担当を割り当てる。
- 言語・音など操作本体を後段で完成させる項目も、旧版の配置を差分表へ記録する。P8aで動かないボタンを公開せず、P8c／P8dで接続した時点に配置を確定する。CSSは該当要素の既存ルールを直し、離れた場所への上書き追加を重ねない。
- 下記「P7までの挙動差分」の周回遷移を先に確認・補修してからメニューへ接続する。入口を公開するだけで、誤った周回・試験へ誘導する状態を完成にしない。

1. `P0_DECISIONS.md`、`P0_DIALOG_INVENTORY.md` と現在の実装を照合し、既に実装・承認済みの復元契約はそのまま採用する。未決項目だけをユーザーへ提示する。棚卸しと計画は未決でも進め、未決事項に依存する実装だけを保留する。
2. `OPEN-001` の提案は「日本語・英語・中国語・韓国語の4言語、到達可能な機能文言・雑学・クイズ・攻略を対象、既存の採用済み新版表現は維持」。これは本計画の追記だけでは決定済みにしない。`OPEN-002` のうち画面配置の是正と採用済み新版表示の維持はユーザー確認済みとして扱い、台帳へ反映する。装飾の厳密な一致範囲は残る確認事項とする。
3. `OPEN-006` は現在のdescriptor名で、開く条件／必要文脈／閉じた先／初期focus／F5後／見本アニメ再開を表にする。追加の閲覧・設定画面は、P0一覧の復元案をたたき台として確定する。リセット・離脱確認はF5で自動実行しない。
4. 旧版の `menuToggle` / `appMenu` を参照し、ステージ、自由、自作、速解き、攻略、ステージ選択、メッセージ、称号、設定、解放後の外部リンクを棚卸しする。P8aでは既存モードへの入口と閉じる動作を実装し、P8b以降の未実装画面へのボタンは公開しない。
5. モードごとの出入りを明示する。自由の途中盤面は既存savedSessionを保持。自作の編集／プレイから離れる場合の保持・破棄と確認有無は旧版を確認して表にする。通常コースへの戻り先は問題ID・周回を含む検証済みの文脈とし、メニューを閉じるだけでは問題を初期化しない。
6. 速解き中にメニューを開くときは時計を停止する。別モードへ移る操作は離脱確認を通し、キャンセルなら同じセッションの一時停止画面へ戻る。確定時の未完走セッションの扱いを明文化し、合格や記録を付与しない。コース試験と通常起動の戻り先を区別する。
7. Tabの移動範囲、Escape／背景クリックで閉じてよい画面、呼び出し元へのfocus復帰、モーダル中の盤面入力抑止をそろえる。必須の節目・失敗・試験ダイアログを汎用closeで飛ばさない。

**完了条件:** 通常→自由→通常、自由→自作→戻る、通常→速解き、速解き離脱のキャンセル／確定が表どおり。メニューの開閉で履歴・報酬・進捗が変わらない。複数モーダルが重ならず、F5で破壊的操作が実行されない。未確定項目は台帳上も未確定とする。

**配置の完了条件:** PC／360pxで共通画面の配置差分を是正済み。後段の画面固有差分には担当段階が明記され、既知の共通配置差が放置されていない。

**コミット境界:** `rebuild(p8a): add main menu and dialog navigation`。

#### P8b. ステージ選択、称号、クリアメッセージ、攻略閲覧

**目的:** 旧版メニューから到達する閲覧機能を、既存ゲーム進行とつなぐ。

- ステージ／周回選択は旧版の表示・選択資格を照合し、未到達コースや未合格試験を迂回できないよう状態側でも検証する。問題を選び直す際の途中盤面の扱いを明示する。
- 称号一覧、クリア後メッセージの前後移動・閲覧可能範囲・閲覧位置を実装する。閲覧はクリア確定や報酬獲得を再実行しない。
- 「攻略のコツ」「盤面の見分け方」「最短2手の9パターン」と詳細・比較表示を旧版と照合する。単なるリンク集で完了にせず、旧版で到達可能な見本操作と戻り方を確認する。
- 雑学・クイズ・型名・回し方は既存カタログを正本にし、閲覧用に別の本文や問題判定を複製しない。見本盤面は表示用の状態を使い、本物の履歴や進捗を変更しない。
- クリア→攻略詳細→クリア、メッセージ→攻略→メッセージの戻り先を保持する。見本アニメーションは閉じる・切り替える際に停止し、古いcallbackが新しい画面を更新しない。
- `all-patterns.html` / `index_3D.html` は旧版の解放条件に沿ったリンクだけ扱う。内容の移植や改修はしない。
- 各一覧・攻略詳細・メッセージ画面の見出し、挿絵・見本盤面、本文、前後／閉じるボタンの順序・寸法・余白を比較し、P8aの担当差分を是正する。既存のクリア内コンテンツも比較対象に含め、採用済みの配置を維持する。

**完了条件:** 一覧・前後・詳細・戻る・F5がP8aの表と一致。閲覧だけでは盤面、時計、進捗、称号、報酬が変化しない。未獲得リンクが開放されない。

**コミット境界:** `rebuild(p8b): add stage and guide browsers`。

#### P8c. 設定と未移植の報酬表示

**目的:** 獲得した見た目を自分で選び直せるようにし、音設定を完成させる。

- 設定一覧、ゲームについて、盤面デザインを実装する。通常／金色／悟りテーマ、通常／30度傾斜配置は現在の描画を使い、設定変更で論理盤面・履歴を作り直さない。
- `themeChosen` / `layoutChosen` と初回報酬自動適用の契約を維持する。未獲得の値はUIと保存検証の両方で拒否する。
- だるま色、虹だるま、覚醒、3Dリンクの資格について旧版の獲得点と新版の不足を照合する。P6/P7実装済みという理由で存在を仮定しない。不足分はこの段階で獲得・保持・表示まで接続し、色の設定値と獲得資格を分ける。
- 回転・クリア音と音on/offを旧版に照合する。ユーザー操作後に音声を開始し、無音設定・バックグラウンド・音声API利用不可でもプレイを続けられるようにする。外部ビルドや音素材の別ファイル依存を追加しない。
- 言語設定のUI・保存項目はP8dと接続する。未翻訳言語を選べるだけの状態を完成とはしない。
- 設定・デザイン・報酬案内の内部配置と、画面上の音ボタンの位置を旧版と揃える。報酬の未獲得／獲得済みで項目数が変わる場合もPC／360pxで確認し、P8aから引き継いだ差分を解消する。

**完了条件:** 獲得前／後／明示選択後／F5後で選択と表示が一致。テーマ・配置・色の変更で解法や当たり判定の対応が変わらない。報酬の再確定でユーザー選択が上書きされない。

**コミット境界:** `rebuild(p8c): finish appearance and sound settings`。

#### P8d. 言語、文言、SVGの移植完了

- P8aで確定した `OPEN-001` の範囲を実装する。対象画面と4言語の欠落一覧を作り、メニュー、設定、ゲーム中の案内、失敗、試験、攻略、雑学、クイズ、称号を確認する。
- 旧版の純粋な翻訳・SVGを再利用してよい。新版で変更したUI文言は現在の意味に合わせる。実装都合で旧版の長い文や不要なボタンを復活させない。
- 文言は機能名や表示文から検索できる位置・名前で管理する。無意味な連番キーや、別の巨大な翻訳フレームワークを導入しない。共通ボタン文言は共有し、機能固有の文言は機能との対応を追えるようにする。
- 言語変更で現在のモード、問題、履歴、dialog文脈を保持する。再描画によってクリア記録や報酬を再付与しない。翻訳欠落時のfallbackと不正言語値の既定値を決める。
- PC／360pxで長い翻訳がボタンや盤面と重ならないかを確認する。SVGの欠落・切れ・アニメ停止も対象とする。
- 言語ボタンの位置、翻訳後の文字サイズ・折返し・行間、SVGと本文・ボタンの並びを旧版と比較して是正する。P8a〜P8cで揃えた日本語画面の配置も再確認する。欠落補充と重なり解消だけで完了にしない。

**完了条件:** 採用した言語範囲に未翻訳・到達不能な本文が残らず、言語を切り替えてF5しても同じプレイ状態へ戻る。範囲外は明示し、勝手に翻訳範囲を縮めない。

**コミット境界:** `rebuild(p8d): complete localized content`。

#### P8e. 通常リセットと完全リセット

- `PERSIST-003` と旧版 `resetStoredProgress` を基に、新版AppStateの各項目を「保持／初期化」で表にする。進捗、周回、覚醒、閲覧記録、報酬、設定、速解き解放・記録・試験資格、自由savedSession、自作sessionを漏らさない。旧版と仕様で判断が割れる項目は実装前に決める。
- 通常リセットは進捗・周回・覚醒・閲覧状態を初期化し、獲得済み盤面報酬、虹だるま・外部リンク資格、設定、速解き解放と記録を保持する。完全リセットはそれらも初期化する。途中セッションと再開先の扱い、初回案内の再表示条件を明記する。
- **現在の実装との調整点:** 速解き解放の一部はコース完了集合から導出している。進捗を消しても解放が残るよう、リセット前に保持すべき解放資格を保存できる契約へ拡張する。表示だけ解放して実際の入口が拒否する状態を作らない。試験合格とメニュー解放を同一視して新周回のゲートを迂回させない。
- リセット確定時に時計・演出・見本・遅延処理を止め、初期状態を一度だけ保存して所定の開始画面へ戻す。キャンセル、連打、直後のF5、hidden復帰で古い状態を書き戻さない。
- リセット確認の見出し・説明・閉じる／通常リセット／完全リセットの位置と余白も旧版と比較する。保持されるものと消えるものが、対応するボタンとともに読める配置にする。
- 保存操作は現行の `APP_STORAGE_KEY`（現在 `wake7-rebuild-v2`）に限定する。過去台帳に記載された仮キーをそのまま実装へ戻さず、旧版キーや同一originの別データを削除しない。schema変更時は現在の新版保存をどう扱うかも明記する。

**完了条件:** 通常／完全それぞれの前後とF5後が保持表どおり。報酬なし／全報酬あり、速解き中、自由／自作中で確認する。`localStorage.clear()` は使わない。

**コミット境界:** `rebuild(p8e): add progress and full reset`。

#### P8f. ダイアログ横断仕上げとP8完了確認

- P8aの復元表を全件埋め、本文・ボタン・戻り先・focus・見本を確認する。単に開くことだけでは完了にしない。
- **旧版と新版を同じ条件で並べて、画面配置を最終確認する。** P8aの配置差分表を全件確認し、各画面の位置・サイズ・並び・余白が旧版または採用済み新版仕様に一致することを確認する。是正対象はP8中に解消する。比較不能・未確認を合格にせず、採用済みの相違は理由を残す。
- 設定→デザイン→設定、クリア→攻略→クリア、速解き→離脱確認→一時停止、リセット確認→キャンセルなど、実際の往復を重点確認する。
- Tab／Shift+Tab、Escape、入力欄中のshortcut抑止、モーダル中の盤面入力禁止、reduced motion、PC／360pxの表示を確認する。テスト用に保存や報酬を上書きする場合は隔離した検証状態を使う。
- 自然な依頼例「速解きから自由へ移る」「虹だるまを選ぶ」「リセットしても記録を残す」「攻略からクリア画面へ戻る」から検索し、主処理・意図・保存・検証へ辿れることを確認する。検索語の暗記を前提にしない。
- `REBUILD_CHECKLIST.md` のC7・C9・Dと対象機能の実装欄を更新する。未実施の実機確認を完了扱いにしない。P9へ渡す残課題と採用済み新版差分を記録する。

**完了条件:** `OPEN-001` / `002` / `006` の採用範囲が確定し、P8a〜P8eの残件が解消している。PC／360pxの配置差分表に未是正・未確認の対象が残っていない。既存盤面エンジンと保存の契約を維持したまま、通常入口から全対象機能へ到達できる。

**コミット境界:** `rebuild(p8f): verify dialogs and settings integration`。P9には自動で進まない。

#### P7までの挙動差分をP8へ取り込む規則

- 計画・実装中に旧版との違いを発見した場合は、再現条件、旧版／新版それぞれの結果、ソース上の入口、対応段階、検証方法をこの節へ追記する。P7以前という理由でP9まで放置しない。
- ユーザーが採用済みの差分（七枚同時の到着演出、応用の対象三枚の発光など）は維持する。旧版との差だけで不具合と決めない。判断が必要なものは確認事項とし、未確認の見た目を一致済みと記録しない。
- 修正は関係するP8段階で行い、必要な状態遷移の補修はその機能の公開より先に済ませる。既存の共通盤面や採用済み演出の作り直しは含めない。

**今回の計画調査で確認した差分（ソース比較。実機再現は未実施）:**

| 差分 | 根拠と現在の状態 | 対応段階・確認する結果 |
| --- | --- | --- |
| 二周目の修行・名人終了も試験要求へ進む | 旧版の `masterDialog` / `MASTER_DIALOG_CHAIN` 周辺では二周目の試験を省略する。新版 `makeTrainingBoundaryDestination` / `makeMasteryBoundaryDestination` は周回を問わず十八番／二十七番要求を返し、`makeSpeedTrialNextAppState` は次コースを一周目で作る。 | **P8aの前半。** 二周目の修行→名人、名人→悟りは周回2を保持して正しい節目へ進む。通常クリア・F5・中断復帰・デバッグで一致し、一周目の試験必須条件を壊さない。 |
| 虹だるまの報酬が未接続 | 旧版 `grantMasterDialogRewardsCommand` は二周目名人到達時に虹だるま資格と初回の色を確定する。新版 `makeDefaultRewards` は金色／悟り資格のみで、虹だるまの保存・描画・選択がない。 | **P8c。** 二周目名人の正規クリア確定に獲得を接続し、初回適用、明示選択、F5、通常／完全リセットまで確認する。閲覧ダイアログを開くだけで付与しない。 |
| 二周目悟り完了が一周目と同じ案内になる | 新版 `makeSatoriBoundaryDestination` は一周目の盤面報酬だけを処理し、`renderSatoriDialog` の完了本文も周回共通で白い盤の獲得を表示する。旧版は覚醒と3Dリンク解放を扱う。 | **P8cで獲得・案内を補完し、P8bの称号／リンクへ接続。** 周回別の本文・次の行き先・資格をそろえる。3DのHTML自体は変更しない。P8fで一連の接続を再確認する。 |

**P8で顕在化する既知の契約差:** 通常リセットで速解き解放を保持する旧版に対し、新版は進捗から解放を導出している。これはP7単独での不具合と断定せず、P8eの保存契約変更として扱う。

この一覧は全P7機能の差分調査完了を意味しない。各P8段階で触る旧版の機能を比較し、追加で見つかった差分も同じ形式で取り込む。

### P9. 統合回帰とRC

- **仕様:** 全仕様ID、チェックリストB〜E、`OPEN-001`〜`OPEN-008`。
- **入力:** P8、現行版、全比較シナリオ、クリーン保存領域。
- **作業:** 全状態自己検査、全縦フロー、重点シナリオ、横断表示、保存破損、連打・中断試験を実施する。未決事項を決定または対象外として記録し、デバッグ入口と公開時の非表示を確認する。
- **完了条件:** チェックリストB〜Eが埋まり、重大差分ゼロ、既知差分は全て `OPEN-*` の決定で説明できる。`index.rebuild.html` 単体をfile/HTTPの両方で起動できる。
- **検証:** 7章・8章の全項目、`git diff --check`、コンソール例外ゼロ、基準版との並列比較。
- **コミット境界:** `rebuild(p9): prepare single-html release candidate`。必要なら `rebuild-v2-rc1` を付ける。
- **停止:** 未説明の挙動差、再現する状態破損、未検証の仕様ID、凍結ファイルの意図しない変更が1件でもある場合。

### P10. 切替と安定化

- **入力:** 合格済みRC、基準タグ、ロールバック手順。
- **作業:** 1コミットで `index.rebuild.html` の内容を公開 `index.html` に切り替え、候補名を消す。切替コミットでは機能変更をしない。公開相当URLでsmoke test後、別コミットで必要な文書整理を行う。
- **完了条件:** リポジトリ上の手編集正本が再び `index.html` 1枚になり、基準タグとRCタグが参照でき、公開相当環境で主要フローが動く。
- **検証:** 初回起動、途中保存、応用開始、九番開始／pause、通常問題クリア、スマホ表示、reduced motionを再確認する。切替コミットのrevertを一時ブランチで試し、旧版が起動することを確認する。
- **コミット境界:** `rebuild: switch index to v2`。安定確認後の文書削除・対象外ファイル整理は別コミット。
- **停止:** RC後にコード差分が入った、rollbackを試せない、基準タグがremoteから取得できない、smoke testが失敗する場合。

## 7. 盤面ロジックと横断検証

### `3^7` ドメイン自己検査

`runSelftest` のような自然な検査関数を公開ビルドで副作用なく実行し、`?debug=1` の時だけ結果を表示する。少なくとも次を全2187状態で検査する。

1. `decode(encode(cells))` と `encode(decode(id))` が恒等で、全IDが一意である。
2. 各セル値が0〜2、各操作対象が相異なる3セル、全軸が盤面ジオメトリの三角に一致する。
3. 全状態 × 全6軸 × 2方向について、roll後も値域と長さを保ち、逆方向rollで元へ戻る。
4. 解決状態ID 0からBFSし、到達数、深さ別件数、各状態の距離を現行版と比較する。現行roll規則の参照値は、到達可能729状態、到達不能1458状態、深さ別 `0:1, 1:12, 2:102, 3:402, 4:212` である。到達不能を距離0などへ丸めず、明示的なsentinelとして扱う。
5. 距離 `d > 0` の到達可能な全状態に、距離を `d-1` へ縮める手が最低1つある。ステージカタログが到達不能状態を参照していない。
6. 全ステージの `state` / `par` / `targetCells` / 指定正解手を検査し、`SOLVER.dist[state] === par`、応用・発展の一手後条件、問題ID重複なしを確認する。
7. 盤面viewの回転・反転はセルの置換として全単射であり、論理盤面や最短距離を変更しない。

自己検査は件数、最初の失敗ID、期待値、実値を出す。単なる `true/false` で終わらせない。現行の計算結果を盲目的に定数化せず、アルゴリズムを別経路で検査する。

### 端末・入力・表示マトリクス

| 軸 | 最低ケース | 主な合否 |
| --- | --- | --- |
| PC | 1280px以上、mouse、keyboard | 盤面・上部UI・dialog非重複、focus順 |
| スマホ | 360px縦、touch、画面回転 | scroll抑止、誤tapなし、文言と時計非重複 |
| motion | 通常 / `prefers-reduced-motion` | 同じ最終状態、入力ロック解除、順序維持 |
| lifecycle | F5、pagehide、hidden→visible | 安定復元、時計一重精算、古いeffect無効 |
| settings | 4言語、音、theme、layout、daruma色 | 保存、解放条件、overflow、音off |
| stress | 連打、pointercancel、遷移中の別操作 | 状態・履歴・DOMの一致、例外ゼロ |

各フェーズで対象行を小さく回し、P9で全組合せの代表シナリオを連続実行する。スマホ確認は狭幅DevToolsだけで終わらせず、少なくとも一度は実タッチ入力で確認する。

## 8. 現行版との比較方法

1. 基準タグの `index.html?debug=1` と作業ブランチの `index.rebuild.html?debug=1` を別URLで同時に開く。保存キーを分け、同じブラウザでも互いの状態を汚染させない。
2. 言語、theme、layout、viewport、motion設定、開始問題を揃える。ランダム問題と速解き順はデバッグ用seedまたは保存済みorderで固定する。
3. 各シナリオに「開始保存状態」「操作列」「期待する安定状態」「撮影時点」を持たせる。操作列は棒番号、方向、リロード時点まで記録する。
4. 静止画比較は盤面セル、棒、軸、メッセージ、紫／水色枠、上部UI、dialogの順に見る。装飾差は `OPEN-002` の範囲外なら不合格にしない。
5. 演出比較は動画または連続スクリーンショットで順序、同時開始、ちらつき、中間空白、最終状態を確認する。秒数そのものは仕様で一致対象の場合だけ比較する。
6. 状態比較は、モード、周回、問題ID、cells、history、clear checkpoint、dialog descriptor、speed status／elapsedをデバッグ読取APIから採取する。DOMのclassを状態値の代用にしない。
7. 差分は「一致」「新版で意図的に安定化」「未説明」の3種に分類する。未説明差分があれば該当フェーズへ戻る。

最優先の比較シナリオは、応用導入全区間、応用誤手巻き戻し、発展／修行の残り2手形、九番候補棒3/4/5本、速解きF5とhidden復帰、各試験境界、クリアdialog中リロードである。

## 9. `OPEN-001`〜`OPEN-008` の決定時期

| 項目 | 決定期限 | 未決時に止める場所 | 初期推奨 |
| --- | --- | --- | --- |
| `OPEN-001` 文言・多言語・コツ範囲 | P0で棚卸し、P8前に確定 | P8 | 現在到達可能な4言語と機能上必要な文言を移植 |
| `OPEN-002` 見た目一致範囲 | P0で基準化、P1/P5/P7ごとに対象確定 | 各対象CSSの受入れ、最終的にP8 | ゲーム性に関わる配置は一致、装飾は近似可 |
| `OPEN-003` 付属HTML | P0 | P0 | V2対象外。リンク契約だけ維持 |
| `OPEN-004` 旧保存キー | P3前 | P3 | 新名前空間で旧値を無視し、削除しない |
| `OPEN-005` 応用導入の再生 | **決定済み** | P5-1〜P5-2 | 履歴0手は再生、1手以上は安定盤面へ直接復元、導入途中は保存しない |
| `OPEN-006` dialog復元 | P0で表作成、clear分はP3前、全件P8前 | P3 / P8 | 壊れた現行復元は再現せず、安全な安定画面を許可 |
| `OPEN-007` 速解きF5後の時計 | **決定済み** | P7a | 通常リロード・hidden復帰ともpausedで復元し、明示再開まで時計を止める |
| `OPEN-008` 離脱後の自由盤面 | **決定済み** | P7d | 最後の有効な自由セッションを永続化し、自由への再入場時に同じ盤面へ戻す |

推奨は決定ではない。現行観測、利用意図、保存複雑度を確認し、採否と理由を仕様台帳に残してから実装する。

## 10. 旧版から移せるもの、作り直すもの

### 検証後にコピー可能

- ステージの数値状態、`par`、応用 `targetCells`、形名と形カタログ、速解き問題プールと表示順の静的データ。
- 多言語文言、クイズ、コツ、節目メッセージなど、`OPEN-001` で採用した純粋コンテンツ。
- だるまのSVG path、静的イラスト、アイコン、メタ情報、外部リンク。
- 盤面座標、色token、ゲーム性に関わる寸法など、`OPEN-002` の比較で妥当性を確認した定数。

コピーは機能単位で行い、データを利用する描画・操作処理の近くに置く。コピーしたデータには件数、ID重複、参照先、配列長を検査する。現行コメントにあるビルド生成前提や未使用データは持ち込まない。

### 制御ロジックとして再実装

- 盤面入力、drag確定、誤手巻き戻し、Undo、Restart、入力ロック。
- 状態所有、進行ゲート、クリア記録、節目route、周回差、UI表示ポリシー。
- 到着、応用導入、形ガイド、クリア、中巻実演を含む全非同期シーケンス。
- 速解き時計、pause、visibility、連続出題、試験合流、リロード正規化。
- 保存形式、妥当性検査、dialog descriptor、復元優先順位、reset。
- modal管理、focus復帰、イベントbinding、DOM参照、描画関数。

旧関数を丸ごと貼って名前だけ変えること、現行の生 `setTimeout` を移植すること、DOMのhidden/classから状態を逆算することは禁止する。純粋ドメイン関数も参照実装として利用できるが、P1の全状態検証に合格して初めて採用扱いにする。

## 11. 切替、ロールバック、削除条件

### 切替条件

- P9完了、全仕様ID確認、全 `OPEN-*` 決定／対象外、重大・高優先の未解決差分ゼロ。
- `rebuild-v2-rc1` のcommitが固定され、基準タグがremoteから取得できる。
- 新版保存を空にした初回導線と、途中保存を持つ復帰導線の両方が合格する。
- `index.rebuild.html` から外部ローカルソースへの参照がなく、単独で開ける。

切替時は旧内容と新版内容を混ぜない。`index.rebuild.html` のRC内容をそのまま `index.html` とし、候補ファイルを同じ切替コミットで除く。旧内容は基準タグとGit履歴に保持されるため、`index.legacy.html` のような第2の手編集正本は作らない。

### ロールバック

- 不具合時は切替コミットをrevertする。緊急公開では基準タグのcommitをデプロイし、修正作業は再構築ブランチで続ける。
- 新旧の保存名前空間を分離しておけば、旧版へ戻しても旧進行を新版が上書きしない。新版キーの削除をロールバック条件にしない。
- rollback検証は切替前に一時ブランチで行い、旧版起動、旧保存読込、主要盤面操作を確認する。

### 旧版・補助文書の削除条件

- 旧 `index.html` 内容が作業ツリーから外れるのは切替条件をすべて満たした時だけ。Gitタグ／履歴からは削除しない。
- `index.rebuild.html` は同一内容が公開 `index.html` に入り、hash比較とsmoke testが済んだ切替コミットでのみ削除する。
- `REBUILD_SPEC.md`、`REBUILD_CHECKLIST.md`、本計画は、切替後の安定確認、既知差分の記録先移管、rollback試験完了後に、別コミットで削除可とする。
- `all-patterns.html`、`index_3D.html` は `OPEN-003` の決定なしに削除・変更しない。

## 12. AI実装タスクの標準依頼テンプレート

```markdown
モデル: Terra high（下記の難所条件に該当する場合のみ Sol high）

目的:
- REBUILD_PLAN P{番号} の {縦スライス名} を完成させる。

対象仕様:
- {仕様IDを列挙}
- 決定済みOPEN: {項目と決定内容}

開始条件:
- 前フェーズcommit: {hash}
- `git status --short` が期待通りであること。
- 未決なら停止する事項: {OPEN-* / 観測事項}

読んでよい範囲:
- `REBUILD_PLAN.md` の P{番号}、依存方向、停止条件
- `REBUILD_SPEC.md` の {仕様ID見出し}
- `REBUILD_CHECKLIST.md` の {A/C/D項目}
- 現行 `index.html` の検索入口: {関数名・定数名・DOM ID}
- 新版 `index.rebuild.html` の入口: {依頼に出る自然語、関数名、DOM ID、CSSクラス}

変更範囲:
- 原則 `index.rebuild.html` の上記入口と、その周辺の関連処理だけ。
- `index.html` と既存仕様文書は変更しない。
- 別CSS/JS、ビルド工程、生成ファイルを作らない。

実装要件:
- {入力→状態遷移→描画→演出→保存の観測可能な流れ}
- {維持する不変条件}
- 依頼に出る自然な言葉、ユーザー向け文言、実装の関数名のいずれから調べても、関連する状態、描画、入力、検査を近い範囲で読めるようにする。検索のためだけのコメント、記号、空の雛形は追加しない。
- 旧版からコピー可: {純粋データ／文言だけを具体化}
- 再実装: {制御ロジックを具体化}

検証:
- {自己検査または操作列}
- PC: {ケース}
- スマホ/タッチ: {ケース}
- リロード/lifecycle: {ケース}
- reduced motion: {ケース}
- 改修しそうな自然語と実装語の双方で検索し、関連箇所へ辿れることを確認する。
- `git diff --check`

停止条件:
- 契約が一意でない、凍結ファイルに差分がある、前フェーズ回帰、
  状態所有の二重化、生timer、未説明の現行差分を発見したら実装を止めて報告する。

完了報告:
- 変更箇所、満たした仕様ID、実行した検証と結果、残件、
  commit候補メッセージを簡潔に報告する。依頼なしにcommit/pushしない。
```

1タスクは1フェーズまたは、同じユーザー操作を入口から保存まで通すさらに小さい縦スライスにする。「CSS全部」「保存全部」のような横割りタスクは避ける。

## 13. モデル運用とPlus枠

- 原則は `Terra high`。静的データ移植、DOM/CSS、通常描画、限定された1機能、決定済み仕様の実装と回帰修正を担当させる。
- `Sol high` は、P2の状態／演出世代、P5の複合導入、P7の速解き時計・lifecycle、P9の統合監査など、複数状態機械が交差する難所か矛盾監査に限定する。最初から全フェーズをSolで実装しない。
- 同じタスクで毎回85万byteの現行HTML全体を再読させない。仕様IDの「根拠」、関数名、定数名、DOM IDを `rg -n` で絞り、前後の必要範囲だけ読む。
- 各フェーズの最初に全仕様を再要約しない。前フェーズcommit、対象ID、既知の関数名・DOM ID・ユーザー向け文言、決定済み `OPEN-*` をプロンプトへ渡す。
- 監査はbranch全体の再説明ではなく、前フェーズcommitからのdiff、対象状態遷移、失敗シナリオに限定する。Solによる全体監査はP9で一度行う。
- マルチエージェントは常用しない。単一HTMLの同時編集は競合と二重設計を生むため、独立した読取専用調査や別端末の検証が明確に分離できる場合だけ使う。
- 失敗時はモデルを上げる前に、再現手順、期待／実際、AppState、RuntimeState、effect generation、対象diffを小さく採取する。同じ曖昧な依頼を高価なモデルへ繰り返さない。
- 各実装タスクは変更と対象検証までで止め、commit、push、次フェーズ着手は明示依頼または受入れ後に分ける。

この運用により、Terraで縦スライスを積み、状態遷移の難所と最終監査にだけSolを使う。モデル選択より先に、読取範囲と完了条件を狭く固定する。
